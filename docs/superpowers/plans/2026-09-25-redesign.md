# Redesign da plataforma: plano de implementação

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** trocar o design system do `app-web` pelo do protótipo (paleta Verde Mata, Ouro, Anil, Urucum e Papel; Bricolage Grotesque e Public Sans; logo da lupa sobre o azulejo), recriar a home e a página do concurso fiéis aos artboards e estender a linguagem às outras páginas.

**Architecture:** os tokens mudam no lugar, em `src/app/globals.css`, mantendo os nomes de papel que os componentes já usam. Os primitivos de `components/ui` são reestilizados e ganham irmãos novos (`Icone`, `Calendario`, `Abas`, `BotaoEmBreve`). A lógica nova (áreas, mapa por UF, fatos do concurso, passos da inscrição, `.ics`, "também abertos") fica em módulos puros de `src/lib`, testados com vitest. Os componentes de página só montam markup a partir deles.

**Tech Stack:** Next 16.3 (App Router, RSC, ISR), React 19.2, Tailwind 4 (`@theme` em CSS), TypeScript, vitest 5, pnpm 11. Chrome headless por CDP em `scripts/verificar-navegador.mjs`.

**Spec:** `docs/superpowers/specs/2026-09-25-redesign-design.md`

## Global Constraints

- **Fonte da verdade visual:** os artboards copiados para `docs/prototipo/` na Task 1. Medidas, cores, raios, sombras e textos saem de lá. "Fiel" quer dizer os mesmos valores em px, não aproximação.
- **Sem travessão** (U+2014) em nenhum texto: UI, comentário, commit, doc. `src/lib/semTravessao.test.ts` já varre `src/`. Use dois pontos, vírgula ou ponto.
- **Sem dependência nova de runtime.** Os ícones são paths Lucide copiados à mão.
- **Next 16 não é o Next do treino.** Antes de usar API de rota, fonte ou imagem, leia o guia em `node_modules/next/dist/docs/` (regra do `AGENTS.md`).
- **Escala de espaço:** `--spacing` volta ao padrão do Tailwind (`0.25rem`, 4 px) e a escala `--text-*` customizada sai, para que `p-5` seja 20 px como no protótipo. Valores fora da escala usam colchetes (`h-[76px]`).
- **Contraste:** todo par texto/fundo passa 4.5:1 e todo par não-texto passa 3:1, nos temas claro, escuro e escuro do sistema, medido por `src/lib/contraste.test.ts`.
- **Placeholder:** botão sem função é `BotaoEmBreve` (abre o aviso "Em breve: {recurso}"); link sem destino vai para `/em-breve/{recurso}` com `noindex`.
- **Preservar:** JSON-LD, `revalidate = 300`, `generateMetadata`, redirects permanentes, `connection()`, o comportamento com a API fora (degradar em vez de lançar) e o drawer de menu do celular.
- **Alvo de toque:** no mínimo 42 px (44 px nos ícones da nav).
- **Idioma:** código, nomes e comentários em português, no tom dos arquivos vizinhos.
- **Verificação por task:** `pnpm verificar` (typecheck, lint e vitest) passa no fim de cada task. As tasks de UI também abrem `http://192.168.15.100:3000` (o dev server já roda em `0.0.0.0:3000`) e comparam com o artboard.
- **Commits:** um por task, mensagem em português no estilo do log (`feat:`, `fix:`, `docs:`), terminando com:
  ```
  Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
  Claude-Session: https://claude.ai/code/session_01H8JivVPNNoz3yuBPUoaskM
  ```

## Review Focus

1. **Concurso sem cargo, sem data ou sem taxa.** Fatos, nota comum, passos e barra fixa precisam mostrar "Não informada" ou omitir o passo, nunca "R$ 0" nem "NaN dias". Testes em `fatos.test.ts` e `inscricao.test.ts` (Tasks 10 e 11).
2. **Acervo vazio ou API fora.** Home com zero abertos não pode dividir por zero no mapa nem renderizar cartão flutuante sem concurso. Testes em `mapaUf.test.ts` (Task 7) e o render vazio da home na Task 9.
3. **Texto longo** (nome de órgão de 90 caracteres, cargo de 120) sem rolagem lateral a 360 px. Coberto por `verificar-navegador.mjs` (Task 16) e por `min-w-0` e `break-words` nos componentes.
4. **Tema escuro nos componentes novos.** Um componente com hex fixo some no escuro. Regra: componente só usa token (`bg-cartao`, `text-tinta-600`...). O único hex fixo permitido fica nos azulejos e no logo, que são arte. Checado pelo grep da Task 16.
5. **Prazo no fuso de São Paulo.** "Encerra hoje" e "falta 1 dia" usam `hojeCivilEmSaoPaulo`, não `new Date()` cru, como já faz a página atual. Testes em `inscricao.test.ts` com a data fixada.

---

## Mapa de arquivos

**Novos**
- `docs/prototipo/{Main,Mobile,Logo,Concurso,ConcursoMobile}.dc.html`: os artboards.
- `public/marca/{horizontal,empilhado,simbolo,simbolo-anil,simbolo-mono}.svg`, `public/icone.svg` (substitui) e `public/icone-32.png` (substitui).
- `src/components/ui/Icone.tsx`: o componente e o mapa de paths.
- `src/components/marca/Azulejos.tsx`: grade de ladrilhos em CSS.
- `src/components/ui/Calendario.tsx`, `src/components/ui/Abas.tsx` e `src/components/ui/EmBreve.tsx` (`BotaoEmBreve` mais o aviso).
- `src/lib/emBreve.ts` (e o teste): os recursos futuros.
- `src/app/em-breve/[recurso]/page.tsx`.
- `src/components/layout/BarraUtilitaria.tsx` e `src/components/layout/NavPrincipal.tsx` (cliente).
- `src/lib/areas.ts`, `src/lib/mapaUf.ts` e os testes.
- `src/components/home/Hero.tsx` (reescrito), `src/components/home/{Mosaico,Numeros,Areas,EncerramSemana,TabelaAbertos,PorEstado,VemAiEDou,ComoFunciona}.tsx`.
- `src/lib/fatos.ts`, `src/lib/inscricao.ts`, `src/lib/agenda.ts` e os testes.
- `src/components/concurso/{CabecalhoDoConcurso,FatosDoConcurso,AcoesDoConcurso,LateralDoConcurso,PassosDaInscricao,TambemAbertos,AbasDoConcurso,BarraDeInscricao}.tsx`.

**Modificados**
- `src/app/globals.css`, `src/app/layout.tsx`, `src/lib/contraste.test.ts` e `src/lib/situacao.ts`.
- `src/components/marca/Logo.tsx`.
- `src/components/ui/{Botao,Cartao,Etiqueta,Campo,Trilha,Paginacao,Sugestoes,Secao}.tsx`.
- `src/components/layout/{Cabecalho,Rodape,SeletorDeTema,MenuConta}.tsx`.
- `src/lib/concursos.ts` (`facetas` com limite e `tambemAbertos`) e `src/lib/concursos.test.ts`.
- `src/app/page.tsx` e `src/components/home/BlocoAlerta.tsx`.
- `src/app/concursos/[slug]/page.tsx` e `src/components/concurso/{Cronograma,Cargos,Faq,AtosPublicados,Avaliacao,LinhaConcurso,CartaoConcurso}.tsx`.
- `src/components/busca/*`, `src/app/orgaos/[slug]/page.tsx`, `src/components/auth/*`, `src/app/{not-found,error,global-error}.tsx`, `src/app/opengraph-image.tsx` e `src/app/estilo/page.tsx`.
- `scripts/verificar-navegador.mjs`.

**Removidos (Task 9)**
- `src/components/home/BlocosSeo.tsx`, `src/components/home/Ilustracao.tsx` e `src/components/home/Secao.tsx` (substituídos).

---

### Task 1: Protótipo no repositório e arquivos da marca

**Files:**
- Create: `docs/prototipo/*.dc.html`, `public/marca/*.svg`
- Modify: `public/icone.svg`, `public/icone-32.png`

**Interfaces:**
- Produces: `docs/prototipo/Main.dc.html` e os outros quatro, citados por número de linha nas tasks seguintes. `public/marca/simbolo.svg` (símbolo 40×40 em cor).

- [ ] **Step 1: Copiar os artboards**

```bash
P=/tmp/claude-1000/-home-vieira-Projects-buscaconcurso/926b89c7-4a4d-4bf0-92f6-9e773a5d0931/scratchpad/artifact-files/57e3e63e-2d9c-4170-a4b1-2fe02712a6b2/project
mkdir -p docs/prototipo && cp $P/{Main,Mobile,Logo,Concurso,ConcursoMobile}.dc.html docs/prototipo/
```

Se a pasta não existir mais, baixe de novo com a ferramenta Artifact (`action: read`, `url: https://claude.ai/artifact/BrUhMCSobNASaWhCDGnbtd`, `paths` com os cinco `project/*.dc.html`).

- [ ] **Step 2: Escrever os SVGs da marca** (copiados de `docs/prototipo/Logo.dc.html:23-40`)

`public/marca/simbolo.svg`:
```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 40 40"><rect width="40" height="40" rx="10" fill="#0B6B3A"/><path d="M25 0H30A10 10 0 0 1 40 10V15A15 15 0 0 1 25 0Z" fill="#F2C230"/><circle cx="17.5" cy="19.5" r="8" fill="none" stroke="#FFFFFF" stroke-width="3.6"/><path d="M23.3 25.3 30 32" stroke="#FFFFFF" stroke-width="3.6" stroke-linecap="round"/></svg>
```
`public/marca/simbolo-anil.svg`: o mesmo, com `rect fill="#1D3F8F"`.
`public/marca/simbolo-mono.svg`: `rect fill="#0F1F17"`, sem o `path` do ouro.
`public/marca/empilhado.svg`: tile `#F6F4EE`, ouro, lupa com `stroke="#0A4D2E"`.
`public/icone.svg`: o do favicon de 32 px (traço 4,4):
```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 40 40"><rect width="40" height="40" rx="10" fill="#0B6B3A"/><path d="M25 0H30A10 10 0 0 1 40 10V15A15 15 0 0 1 25 0Z" fill="#F2C230"/><circle cx="17.5" cy="19.5" r="8" fill="none" stroke="#FFFFFF" stroke-width="4.4"/><path d="M23.3 25.3 30 32" stroke="#FFFFFF" stroke-width="4.4" stroke-linecap="round"/></svg>
```
`public/marca/horizontal.svg`: símbolo mais a palavra como `<text>` em Bricolage Grotesque 700 (`viewBox="0 0 260 40"`, símbolo em `x=0`, texto em `x=50 y=29 font-size=26 letter-spacing=-0.9`, "Busca" em `#0F1F17` e `<tspan fill="#0B6B3A">Concurso</tspan>`). É só para uso externo, porque o site desenha o logo inline (Task 4).

- [ ] **Step 3: Gerar o PNG de 32 px pelo Chrome headless**

```bash
cd public && google-chrome --headless=new --disable-gpu --hide-scrollbars \
  --default-background-color=00000000 --window-size=32,32 \
  --screenshot=icone-32.png "file://$PWD/icone.svg" && file icone-32.png
```
Expected: `PNG image data, 32 x 32, 8-bit/color RGBA`.

- [ ] **Step 4: Conferir e fazer o commit**

Abra `http://192.168.15.100:3000/icone.svg` e confira a lupa sobre o azulejo com o canto em ouro.
```bash
git add docs/prototipo public/marca public/icone.svg public/icone-32.png
git commit -m "feat: protótipo do redesign no repositório e arquivos da marca nova"
```

---

### Task 2: Tokens, fontes e contraste

**Files:**
- Modify: `src/app/globals.css:1-400` (bloco `@theme` e os dois blocos do escuro), `src/app/layout.tsx:2,20-31,153`
- Test: `src/lib/contraste.test.ts`

**Interfaces:**
- Produces (utilitários Tailwind usados daqui em diante):
  - superfícies: `bg-pagina` `bg-cartao` `bg-rebaixada` `bg-faixa` (Verde Noite) `bg-utilitaria` (`#06331E`)
  - linhas: `linha` (`#E3DFD4`), `linha-fraca` (`#EEEBE3`), `contorno` (`#D4CFC2`)
  - tinta: `tinta-900` `tinta-600` `tinta-500` (os outros degraus saem e o grep da Step 6 troca os usos)
  - ação: `acao` `acao-hover` `acao-texto` `link` `link-hover`
  - chamada: `ouro` `ouro-hover` `ouro-texto`
  - sinais (cada um com `-fundo` e `-texto`): `verde`, `anil`, `ouro`, `urucum`, `neutro`, por exemplo `bg-urucum-fundo text-urucum-texto`, e as cores cheias `bg-urucum` `bg-anil` `bg-verde`
  - `utilitaria-texto` (`#CFE3D6`), `faixa-texto` (`#CFE3D6`), `ouro-faixa` (`#C99A00`)
  - fontes: `font-titulo` (Bricolage Grotesque), `font-interface` (Public Sans)
  - sombras: `shadow-cartao`, `shadow-numeros`, `shadow-flutuante`, `shadow-aba`, `shadow-busca`
  - raios: `rounded-controle` (11px), `rounded-cartao` (18px), `rounded-painel` (24px)
  - tons de situação mantidos por nome (`urgente`, `previsto`, `encerrado` e os `-chip`), agora apontando para urucum, ouro e neutro

- [ ] **Step 1: Escrever o teste de contraste novo (falha)**

Em `src/lib/contraste.test.ts`, troque `SUPERFICIES` e `PARES` por:

```ts
const SUPERFICIES = ["pagina", "cartao", "rebaixada"];

const PARES: Par[] = [
  ...["tinta-900", "tinta-600", "tinta-500"].flatMap((texto) =>
    SUPERFICIES.map((fundo) => ({ texto, fundo, minimo: TEXTO })),
  ),
  ...["link", "link-hover", "acao"].flatMap((texto) =>
    ["pagina", "cartao"].map((fundo) => ({ texto, fundo, minimo: TEXTO })),
  ),
  { texto: "acao-texto", fundo: "acao", minimo: TEXTO },
  { texto: "acao-texto", fundo: "acao-hover", minimo: TEXTO },
  { texto: "ouro-texto", fundo: "ouro", minimo: TEXTO },
  { texto: "ouro-texto", fundo: "ouro-hover", minimo: TEXTO },
  ...["verde", "anil", "ouro", "urucum", "neutro"].map((sinal) => ({
    texto: `${sinal}-texto`, fundo: `${sinal}-fundo`, minimo: TEXTO,
  })),
  { texto: "urucum-texto", fundo: "cartao", minimo: TEXTO },
  { texto: "faixa-texto", fundo: "faixa", minimo: TEXTO },
  { texto: "utilitaria-texto", fundo: "utilitaria", minimo: TEXTO },
  { texto: "acao-texto", fundo: "urucum", minimo: TEXTO },
  { texto: "acao-texto", fundo: "anil", minimo: TEXTO },
  { texto: "rodape-texto", fundo: "rodape", minimo: TEXTO },
  { texto: "rodape-suave", fundo: "rodape", minimo: TEXTO },
  // Não texto: a superfície do botão primário, o anel de foco e os ícones.
  { texto: "acao", fundo: "cartao", minimo: NAO_TEXTO },
  { texto: "acao", fundo: "pagina", minimo: NAO_TEXTO },
  { texto: "ouro", fundo: "faixa", minimo: NAO_TEXTO },
  { texto: "contorno", fundo: "cartao", minimo: 1.3 },
];
```
O `contorno` a 1,3:1 é decorativo: o botão de contorno também tem texto, e ele passa 4.5 sozinho. Mantenha os testes de `razaoDeContraste` e o laço que roda `PARES` nos três temas como estão.

- [ ] **Step 2: Rodar e ver falhar**

Run: `pnpm vitest run src/lib/contraste.test.ts`
Expected: FAIL com `token "ouro-texto" ausente` (ou equivalente de `lerCor` com `undefined`).

- [ ] **Step 3: Reescrever o `@theme` de `globals.css`**

Troque o bloco `@theme { ... }` inteiro (linhas 1 a ~121) e o comentário de cabeça por:

```css
@import "tailwindcss";

/*
 * Tokens do protótipo "BuscaConcurso · Home Redesign" (docs/prototipo).
 *
 * A marca vem de Athos Bulcão: azulejo verde, sol em ouro, papel de edital.
 * O ouro é a chamada única da tela. Urucum é prazo curto. Anil é link e
 * informação. Os nomes são de papel e não de cor: o escuro troca o valor, o
 * componente não muda.
 */
@theme {
  --color-pagina: #f6f4ee;
  --color-cartao: #ffffff;
  --color-rebaixada: #f1eee6;
  --color-faixa: #0a4d2e;
  --color-faixa-texto: #cfe3d6;
  --color-utilitaria: #06331e;
  --color-utilitaria-texto: #cfe3d6;

  --color-linha: #e3dfd4;
  --color-linha-fraca: #eeebe3;
  --color-contorno: #d4cfc2;

  --color-tinta-900: #0f1f17;
  --color-tinta-600: #4f5d55;
  --color-tinta-500: #66736b;

  --color-acao: #0b6b3a;
  --color-acao-hover: #0a5c32;
  --color-acao-texto: #ffffff;
  --color-link: #1d3f8f;
  --color-link-hover: #0b6b3a;

  --color-ouro: #f2c230;
  --color-ouro-hover: #e6b422;
  --color-ouro-texto: #2a2000;
  --color-ouro-faixa: #c99a00;

  --color-verde: #0b6b3a;
  --color-verde-fundo: #e4f1e8;
  --color-verde-texto: #0b6b3a;
  --color-anil: #1d3f8f;
  --color-anil-fundo: #e7ecf8;
  --color-anil-texto: #1d3f8f;
  --color-ouro-fundo: #fcf1cf;
  --color-ouro-sinal-texto: #7a5b00;
  --color-urucum: #b8401f;
  --color-urucum-fundo: #fbe7e0;
  --color-urucum-texto: #8f2f15;
  --color-neutro-fundo: #f1eee6;
  --color-neutro-texto: #0f1f17;

  /* Situação do concurso, pelo nome antigo, para situacao.ts e os cartões. */
  --color-urgente: var(--color-urucum-fundo);
  --color-urgente-chip: var(--color-urucum-fundo);
  --color-previsto: var(--color-ouro-fundo);
  --color-previsto-chip: var(--color-ouro-fundo);
  --color-encerrado: var(--color-rebaixada);
  --color-encerrado-chip: var(--color-rebaixada);

  --color-rodape: #ffffff;
  --color-rodape-texto: #0f1f17;
  --color-rodape-suave: #4f5d55;

  --font-titulo: var(--fonte-bricolage), "Helvetica Neue", Arial, sans-serif;
  --font-interface: var(--fonte-public-sans), system-ui, sans-serif;

  --radius-controle: 11px;
  --radius-cartao: 18px;
  --radius-painel: 24px;

  --shadow-cartao: 0 1px 0 var(--color-linha);
  --shadow-numeros: 0 1px 0 var(--color-linha), 0 20px 50px rgb(15 31 23 / 0.08);
  --shadow-tabela: 0 1px 0 var(--color-linha), 0 12px 40px rgb(15 31 23 / 0.05);
  --shadow-flutuante: 0 30px 60px rgb(0 30 15 / 0.35);
  --shadow-busca: 0 24px 60px rgb(0 30 15 / 0.35);
  --shadow-aba: 0 1px 3px rgb(15 31 23 / 0.12);
}
```

O sinal ouro tem dois textos: `ouro-texto` (`#2A2000`, sobre o ouro cheio) e `ouro-sinal-texto` (`#7A5B00`, sobre `ouro-fundo`). No teste, o par do sinal ouro é `{texto: "ouro-sinal-texto", fundo: "ouro-fundo"}`. Troque a linha gerada para `ouro` no `map` da Step 1 por esse par explícito.

- [ ] **Step 4: Reescrever os dois blocos do escuro**

Nos dois blocos (`:root[data-tema="escuro"], ...` e o `@media (prefers-color-scheme: dark)`), troque as variáveis por este conjunto (os valores são idênticos nos dois):

```css
  color-scheme: dark;
  --color-pagina: #0b1611;
  --color-cartao: #13211a;
  --color-rebaixada: #1c2d24;
  --color-faixa: #072a19;
  --color-faixa-texto: #cfe3d6;
  --color-utilitaria: #041a0f;
  --color-utilitaria-texto: #cfe3d6;
  --color-linha: #22362b;
  --color-linha-fraca: #1a2a21;
  --color-contorno: #34493d;
  --color-tinta-900: #eef2ee;
  --color-tinta-600: #b3c0b8;
  --color-tinta-500: #97a59c;
  --color-acao: #0b6b3a;
  --color-acao-hover: #0e7a43;
  --color-acao-texto: #ffffff;
  --color-link: #8fb0f0;
  --color-link-hover: #7fd3a2;
  --color-ouro-hover: #f5cd4f;
  --color-verde-fundo: #16382a;
  --color-verde-texto: #7fd3a2;
  --color-anil-fundo: #1a2744;
  --color-anil-texto: #a9c0f2;
  --color-ouro-fundo: #33290b;
  --color-ouro-sinal-texto: #f2c230;
  --color-urucum: #b8401f;
  --color-urucum-fundo: #3a1c12;
  --color-urucum-texto: #f4a58c;
  --color-neutro-fundo: #1c2d24;
  --color-neutro-texto: #eef2ee;
  --color-rodape: #13211a;
  --color-rodape-texto: #eef2ee;
  --color-rodape-suave: #b3c0b8;
```

- [ ] **Step 5: Escala de espaço, texto e regras globais**

Ainda em `globals.css`:
- Apague `--spacing`, `--text-*` e `--radius-caixa` do `@theme`, e apague também o comentário grande logo abaixo sobre a escala de 0,22rem.
- Troque o bloco `.numero` por `body { font-variant-numeric: tabular-nums; }` (o protótipo liga os tabulares no corpo todo) e mantenha a classe `.numero` como alias vazio (`.numero {}`), para não quebrar quem usa.
- Apague `.aurora` e os `@keyframes`/`@property` dela: o hero novo não tem anel.
- Mantenha `:focus-visible`, `[data-sem-anel]` e o bloco do `Revelador`.
- Adicione:
```css
input::placeholder { color: var(--color-tinta-500); }
.sr-only-foco:not(:focus) { position: absolute; width: 1px; height: 1px; overflow: hidden; clip: rect(0 0 0 0); white-space: nowrap; }
```

- [ ] **Step 6: Trocar as fontes no layout**

Em `src/app/layout.tsx`:
```ts
import { Bricolage_Grotesque, Public_Sans } from "next/font/google";

const bricolage = Bricolage_Grotesque({
  subsets: ["latin", "latin-ext"],
  weight: ["500", "600", "700", "800"],
  variable: "--fonte-bricolage",
  display: "swap",
});

const publicSans = Public_Sans({
  subsets: ["latin", "latin-ext"],
  weight: ["400", "500", "600", "700"],
  variable: "--fonte-public-sans",
  display: "swap",
});
```
e `className={`${bricolage.variable} ${publicSans.variable} h-full antialiased`}`. Confira o nome exato do export em `node_modules/next/dist/compiled/@next/font/dist/google/index.d.ts` (`grep -n "Bricolage\|Public_Sans"`).

- [ ] **Step 7: Trocar os tokens que saíram**

```bash
grep -rnoE "(bg|text|border|ring|outline|fill|stroke|from|to|via|divide|shadow|decoration)-(tinta-(800|400|300|200|100)|verde-(900|700|600|500|300)|amarelo(-escuro|-texto|-hover)?|ocre|vermelho(-800)?|bloco|escura|inverso(-hover|-texto)?|urgente-apoio|previsto-(texto|apoio)|marca-[a-z]+|rodape-(tenue|faixa))\b|rounded-caixa" src | cut -d: -f1 | sort | uniq -c
```
Troque em cada arquivo listado:

| Antigo | Novo |
|---|---|
| `tinta-800` | `tinta-900` |
| `tinta-400` | `tinta-500` |
| `tinta-300`, `tinta-200`, `tinta-100` (borda, fundo) | `linha`, `linha`, `rebaixada` |
| `verde-700`, `verde-600` | `acao`, `acao-hover` |
| `verde-500`, `verde-300` | `verde` |
| `verde-900` | `faixa` |
| `amarelo`, `amarelo-escuro`/`-hover`, `amarelo-texto` | `ouro`, `ouro-hover`, `ouro-texto` |
| `ocre`, `previsto-texto`, `previsto-apoio` | `ouro-sinal-texto` |
| `vermelho`, `vermelho-800`, `urgente-apoio` | `urucum`, `urucum-texto`, `urucum-texto` |
| `bloco` | `rebaixada` |
| `escura`, `inverso` | `tinta-900` |
| `inverso-texto` | `cartao` |
| `inverso-hover` | `tinta-600` |
| `rodape-tenue` | `rodape-suave` |
| `rodape-faixa` | `linha-fraca` |
| `rounded-caixa` | `rounded-cartao` |
| `marca-*` | (some na Task 4, com o `Logo` novo; troque aqui por `acao` para compilar) |

Rode o grep de novo. Expected: nenhuma linha.

- [ ] **Step 8: Rodar o teste de contraste e ajustar valores**

Run: `pnpm vitest run src/lib/contraste.test.ts`
Expected: PASS. Se algum par falhar, escureça o texto (claro) ou clareie o texto (escuro) em passos de 4 por canal até passar, e anote no comentário do token o valor do protótipo e o motivo do ajuste. Os candidatos conhecidos são `tinta-500` sobre `rebaixada` (o protótipo usa `#66736B` sobre `#F1EEE6`, que fica perto de 4.5) e o `link-hover` escuro.

- [ ] **Step 9: Verificar e fazer o commit**

Run: `pnpm verificar`
Expected: PASS. Abra `http://192.168.15.100:3000`: Papel no fundo, Public Sans no texto, Bricolage nos títulos, nada ilegível. O layout vai estar estranho até as próximas tasks, e isso é esperado.
```bash
git add -A src/app/globals.css src/app/layout.tsx src/lib/contraste.test.ts src
git commit -m "feat: tokens, fontes e contraste do design system novo"
```

---

### Task 3: Ícones

**Files:**
- Create: `src/components/ui/Icone.tsx`
- Test: `src/components/ui/Icone.test.ts`

**Interfaces:**
- Produces: `type NomeDoIcone`, `Icone({ nome, tamanho = 20, traco = 1.75, className, rotulo }: { nome: NomeDoIcone; tamanho?: number; traco?: number; className?: string; rotulo?: string })`. Sem `rotulo` ele é `aria-hidden`; com `rotulo`, é `role="img"` e `aria-label`.

- [ ] **Step 1: Teste (falha)**

```ts
import { createElement } from "react";
import { renderToStaticMarkup } from "react-dom/server";
import { describe, expect, it } from "vitest";
import { Icone, ICONES } from "./Icone";

describe("Icone", () => {
  it("sem rótulo é decorativo", () => {
    const html = renderToStaticMarkup(createElement(Icone, { nome: "busca" }));
    expect(html).toContain('aria-hidden="true"');
    expect(html).toContain('stroke-width="1.75"');
    expect(html).toContain('width="20"');
  });

  it("com rótulo vira imagem com nome", () => {
    const html = renderToStaticMarkup(createElement(Icone, { nome: "salvar", rotulo: "Salvar" }));
    expect(html).toContain('role="img"');
    expect(html).toContain('aria-label="Salvar"');
  });

  it("todo ícone tem ao menos um traço", () => {
    for (const [nome, partes] of Object.entries(ICONES)) {
      expect(partes.length, nome).toBeGreaterThan(0);
    }
  });
});
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `pnpm vitest run src/components/ui/Icone.test.ts`
Expected: FAIL, `Cannot find module './Icone'`.

- [ ] **Step 3: Implementar**

Cada ícone é uma lista de elementos SVG. Os paths são copiados de `docs/prototipo/Logo.dc.html:62-99` e `Main.dc.html` (nav, hero e números). O modelo:

```tsx
import type { JSX } from "react";

type Parte =
  | { d: string; preenchido?: boolean }
  | { circulo: [number, number, number] }
  | { retangulo: [number, number, number, number, number?] };

/** Paths Lucide (ISC), copiados do protótipo. Traço 1,75, 24 px. */
export const ICONES = {
  busca: [{ circulo: [11, 11, 7] }, { d: "m20 20-3.5-3.5" }],
  tribunais: [{ d: "M12 3v18M7 21h10M5 7h14" }, { d: "m5 7-3 7a3.5 3.5 0 0 0 6 0Z" }, { d: "m19 7-3 7a3.5 3.5 0 0 0 6 0Z" }],
  policia: [{ d: "M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10Z" }, { d: "m9 12 2 2 4-4" }],
  educacao: [{ d: "M22 10 12 5 2 10l10 5 10-5Z" }, { d: "M6 12v5c3 2 9 2 12 0v-5" }, { d: "M22 10v6" }],
  saude: [{ d: "M19 14c1.5-1.5 3-3.2 3-5.5A5.5 5.5 0 0 0 16.5 3c-1.8 0-3 .5-4.5 2-1.5-1.5-2.7-2-4.5-2A5.5 5.5 0 0 0 2 8.5c0 2.3 1.5 4 3 5.5l7 7Z" }, { d: "M3.5 12h5l1.5-3 2 6 1.5-3h7" }],
  fiscal: [{ retangulo: [4, 2, 16, 20, 2] }, { d: "M8 6h8M8 11h.01M12 11h.01M16 11h.01M8 15h.01M12 15h.01M16 15v3M8 18h.01M12 18h.01" }],
  estatais: [{ d: "M3 22h18M6 18v-7M10 18v-7M14 18v-7M18 18v-7" }, { d: "M12 2 20 7H4Z" }],
  forcas: [{ circulo: [12, 5, 3] }, { d: "M12 22V8M5 12H2a10 10 0 0 0 20 0h-3" }],
  prefeituras: [{ d: "M6 22V4a2 2 0 0 1 2-2h8a2 2 0 0 1 2 2v18Z" }, { d: "M6 12H4a2 2 0 0 0-2 2v8h4M18 9h2a2 2 0 0 1 2 2v11h-4M10 6h4M10 10h4M10 14h4M10 18h4" }],
  conselhos: [{ retangulo: [2, 5, 20, 14, 2] }, { circulo: [8, 11, 2] }, { d: "M5 16c.5-1.5 1.7-2 3-2s2.5.5 3 2M14 10h4M14 14h3" }],
  tecnologia: [{ retangulo: [5, 5, 14, 14, 2] }, { retangulo: [9, 9, 6, 6] }, { d: "M9 2v3M15 2v3M9 19v3M15 19v3M2 9h3M2 15h3M19 9h3M19 15h3" }],
  administrativo: [{ retangulo: [2, 7, 20, 14, 2] }, { d: "M16 7V5a2 2 0 0 0-2-2h-4a2 2 0 0 0-2 2v2M2 13h20" }],
  ambiente: [{ d: "M7 20h10M12 20v-8" }, { d: "M12 12c0-4 3-7 8-7 0 4-3 7-8 7Z" }, { d: "M12 14c0-3.5-2.5-6-7-6 0 3.5 2.5 6 7 6Z" }],
  salario: [{ retangulo: [2, 6, 20, 12, 2] }, { circulo: [12, 12, 2.5] }, { d: "M6 12h.01M18 12h.01" }],
  vagas: [{ d: "M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2" }, { circulo: [9, 7, 4] }, { d: "M22 21v-2a4 4 0 0 0-3-3.87M16 3.13a4 4 0 0 1 0 7.75" }],
  pessoa: [{ circulo: [9, 7, 4] }, { d: "M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2" }],
  prazo: [{ circulo: [12, 13, 8] }, { d: "M12 9v4l2 2M5 3 2 6M22 6l-3-3" }],
  local: [{ d: "M20 10c0 6-8 12-8 12s-8-6-8-12a8 8 0 0 1 16 0Z" }, { circulo: [12, 10, 3] }],
  banca: [{ d: "M5 22h14M19.3 14H4.7a1.7 1.7 0 0 0-1.7 1.7V18h18v-2.3a1.7 1.7 0 0 0-1.7-1.7Z" }, { d: "M14 14v-3a3 3 0 0 1 1-2.2 4 4 0 1 0-6 0A3 3 0 0 1 10 11v3" }],
  diario: [{ d: "M4 22h16a2 2 0 0 0 2-2V4a2 2 0 0 0-2-2H8a2 2 0 0 0-2 2v16a2 2 0 0 1-2 2Zm0 0a2 2 0 0 1-2-2v-9c0-1.1.9-2 2-2h2" }, { d: "M18 14h-8M15 18h-5M10 6h8v4h-8Z" }],
  aberto: [{ d: "M15 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V7Z" }, { d: "M14 2v4a2 2 0 0 0 2 2h4" }, { d: "m9 15 2 2 4-4" }],
  previsto: [{ retangulo: [3, 4, 18, 18, 2] }, { d: "M16 2v4M8 2v4M3 10h18" }],
  homologado: [{ d: "M3.85 8.62a4 4 0 0 1 4.78-4.77 4 4 0 0 1 6.74 0 4 4 0 0 1 4.78 4.78 4 4 0 0 1 0 6.74 4 4 0 0 1-4.77 4.78 4 4 0 0 1-6.75 0 4 4 0 0 1-4.78-4.77 4 4 0 0 1 0-6.76Z" }, { d: "m9 12 2 2 4-4" }],
  alerta: [{ d: "M6 8a6 6 0 0 1 12 0c0 7 3 9 3 9H3s3-2 3-9" }, { d: "M10.3 21a1.94 1.94 0 0 0 3.4 0" }],
  salvar: [{ d: "m19 21-7-4-7 4V5a2 2 0 0 1 2-2h10a2 2 0 0 1 2 2Z" }],
  areas: [{ retangulo: [3, 3, 7, 7, 1.5] }, { retangulo: [14, 3, 7, 7, 1.5] }, { retangulo: [3, 14, 7, 7, 1.5] }, { retangulo: [14, 14, 7, 7, 1.5] }],
  estados: [{ d: "M14.1 6 9.9 4 3 7v13l6.9-3 4.2 2L21 17V4Z" }, { d: "M9.9 4v13M14.1 6v13" }],
  entrar: [{ circulo: [12, 8, 4] }, { d: "M4 21a8 8 0 0 1 16 0" }],
  seta: [{ d: "M5 12h14M12 5l7 7-7 7" }],
  externo: [{ d: "M7 17 17 7M7 7h10v10" }],
  abaixo: [{ d: "m6 9 6 6 6-6" }],
  acima: [{ d: "m18 15-6-6-6 6" }],
  voltar: [{ d: "m15 18-6-6 6-6" }],
  mira: [{ circulo: [12, 12, 7] }, { d: "M12 2v3M12 19v3M2 12h3M19 12h3" }, { circulo: [12, 12, 2] }],
  lupaDocumento: [{ d: "M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h5" }, { d: "M14 2v4a2 2 0 0 0 2 2h4v3" }, { circulo: [16.5, 16.5, 3.5] }, { d: "m21 21-1.9-1.9" }],
  ciclo: [{ d: "M21 12a9 9 0 0 1-15 6.7L3 16" }, { d: "M3 12a9 9 0 0 1 15-6.7L21 8" }, { d: "M21 3v5h-5M3 21v-5h5" }],
  ordenar: [{ d: "M3 6h18M6 12h12M10 18h4" }],
  contraste: [{ circulo: [12, 12, 9] }, { d: "M12 3a9 9 0 0 0 0 18Z", preenchido: true }],
  compartilhar: [{ circulo: [18, 5, 3] }, { circulo: [6, 12, 3] }, { circulo: [18, 19, 3] }, { d: "m8.6 13.5 6.8 4M15.4 6.5l-6.8 4" }],
  check: [{ d: "M20 6 9 17l-5-5" }],
  relogio: [{ circulo: [12, 12, 10] }, { d: "M12 6v6l4 2" }],
  recibo: [{ d: "M4 2v20l3-2 3 2 2-2 2 2 3-2 3 2V2l-3 2-3-2-2 2-2-2-3 2Z" }, { d: "M8 9h8M8 13h6" }],
  globo: [{ circulo: [12, 12, 10] }, { d: "M2 12h20M12 2a15 15 0 0 1 0 20M12 2a15 15 0 0 0 0 20" }],
  sol: [{ circulo: [12, 12, 4] }, { d: "M12 2v2M12 20v2M4.9 4.9l1.4 1.4M17.7 17.7l1.4 1.4M2 12h2M20 12h2M4.9 19.1l1.4-1.4M17.7 6.3l1.4-1.4" }],
  lua: [{ d: "M12 3a6 6 0 0 0 9 9 9 9 0 1 1-9-9Z" }],
  monitor: [{ retangulo: [2, 3, 20, 14, 2] }, { d: "M8 21h8M12 17v4" }],
  fechar: [{ d: "M18 6 6 18M6 6l12 12" }],
  menu: [{ d: "M4 6h16M4 12h16M4 18h16" }],
} satisfies Record<string, Parte[]>;

export type NomeDoIcone = keyof typeof ICONES;

export function Icone({
  nome,
  tamanho = 20,
  traco = 1.75,
  className,
  rotulo,
}: {
  nome: NomeDoIcone;
  tamanho?: number;
  traco?: number;
  className?: string;
  rotulo?: string;
}): JSX.Element {
  const partes: Parte[] = ICONES[nome];
  return (
    <svg
      width={tamanho}
      height={tamanho}
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      strokeWidth={traco}
      strokeLinecap="round"
      strokeLinejoin="round"
      className={`shrink-0 ${className ?? ""}`}
      {...(rotulo ? { role: "img", "aria-label": rotulo } : { "aria-hidden": true })}
    >
      {partes.map((parte, i) => {
        if ("circulo" in parte) {
          const [cx, cy, r] = parte.circulo;
          return <circle key={i} cx={cx} cy={cy} r={r} />;
        }
        if ("retangulo" in parte) {
          const [x, y, width, height, rx] = parte.retangulo;
          return <rect key={i} x={x} y={y} width={width} height={height} rx={rx} />;
        }
        return <path key={i} d={parte.d} fill={parte.preenchido ? "currentColor" : undefined} />;
      })}
    </svg>
  );
}
```
Os ícones do tema (`sol`, `lua`, `monitor`), `fechar` e `menu` não estão no protótipo: são os do Lucide com o mesmo traço. Se `SeletorDeTema.tsx` ou `Revelador.tsx` já desenham esses ícones inline, troque por `<Icone nome=...>` na task em que o arquivo for mexido.

- [ ] **Step 4: Rodar e ver passar**

Run: `pnpm vitest run src/components/ui/Icone.test.ts`
Expected: PASS (3 testes).

- [ ] **Step 5: Commit**

```bash
git add src/components/ui/Icone.tsx src/components/ui/Icone.test.ts
git commit -m "feat: ícones do protótipo como componente, traço 1,75"
```

---

### Task 4: Logo e azulejos

**Files:**
- Modify: `src/components/marca/Logo.tsx` (reescrita)
- Create: `src/components/marca/Azulejos.tsx`
- Test: `src/components/marca/Azulejos.test.ts`

**Interfaces:**
- Consumes: tokens da Task 2.
- Produces:
  - `Logo({ tamanho = 36, variante = "horizontal", tom = "cor", className })`, com `variante: "horizontal" | "empilhado" | "simbolo"` e `tom: "cor" | "claro" | "mono"` (as mesmas exportações de tipo de antes: `TomDaMarca`, `VarianteDaMarca`). `tamanho` é o lado do símbolo em px. A palavra usa `font-titulo`, 700, `tracking-[-0.02em]` e `fontSize = tamanho * 0.58` (21 px quando `tamanho=36`, como na nav).
  - `type Ladrilho = { fundo: CorDeAzulejo; circulo: { x: string; y: string; d: string; cor: CorDeAzulejo } }`, `type CorDeAzulejo = "verde" | "verdeAzulejo" | "ouro" | "anil" | "papel"`.
  - `MOSAICO_HERO: Ladrilho[]` (16), `FAIXA_MARCA: Ladrilho[]` (12).
  - `Azulejos({ ladrilhos, colunas, className })`: `aria-hidden`, `overflow-hidden`, cada ladrilho `relative overflow-hidden` com o círculo `absolute rounded-full`.

- [ ] **Step 1: Teste (falha)**

```ts
import { createElement } from "react";
import { renderToStaticMarkup } from "react-dom/server";
import { describe, expect, it } from "vitest";
import { Azulejos, FAIXA_MARCA, MOSAICO_HERO } from "./Azulejos";

describe("Azulejos", () => {
  it("o mosaico do hero é 4×4 e a faixa da marca é 6×2", () => {
    expect(MOSAICO_HERO).toHaveLength(16);
    expect(FAIXA_MARCA).toHaveLength(12);
  });

  it("é decorativo e desenha um círculo por ladrilho", () => {
    const html = renderToStaticMarkup(
      createElement(Azulejos, { ladrilhos: MOSAICO_HERO, colunas: 4 }),
    );
    expect(html).toContain('aria-hidden="true"');
    expect(html.match(/rounded-full/g)).toHaveLength(16);
  });
});
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `pnpm vitest run src/components/marca/Azulejos.test.ts`
Expected: FAIL, módulo ausente.

- [ ] **Step 3: Implementar `Azulejos.tsx`**

As cores são arte e ficam fixas nos dois temas (é o único hex permitido fora dos tokens, junto com o logo):
```tsx
const COR = {
  verde: "#0B6B3A",
  verdeAzulejo: "#0E5C35",
  ouro: "#F2C230",
  anil: "#1D3F8F",
  papel: "#F6F4EE",
} as const;
```
Os ladrilhos em porcentagem da célula. O hero tem células de 112 px, então 224 px vira `200%`, 112 vira `100%`, 56 vira `50%`, 64 vira `57.14%`, 40 vira `35.71%`, 24 vira `21.43%` e 36 vira `32.14%`. Transcreva `docs/prototipo/Main.dc.html:83-98` na ordem, por exemplo:
```ts
export const MOSAICO_HERO: Ladrilho[] = [
  { fundo: "papel", circulo: { x: "0%", y: "0%", d: "200%", cor: "verde" } },
  { fundo: "ouro", circulo: { x: "0%", y: "50%", d: "100%", cor: "anil" } },
  { fundo: "verde", circulo: { x: "-100%", y: "-100%", d: "200%", cor: "ouro" } },
  { fundo: "anil", circulo: { x: "21.43%", y: "21.43%", d: "57.14%", cor: "papel" } },
  // ... as outras 12 linhas de Main.dc.html:87-98, com a mesma conversão
];
```
A `FAIXA_MARCA` sai de `docs/prototipo/Logo.dc.html:46-57` com a mesma regra (lá a célula mede 110 px: 220 é `200%`, 110 é `100%`, 55 é `50%`, e `25%`/`50%` e `30%`/`40%` ficam como estão).

O componente:
```tsx
export function Azulejos({ ladrilhos, colunas, className }: { ladrilhos: Ladrilho[]; colunas: number; className?: string }) {
  return (
    <div aria-hidden="true" className={`grid overflow-hidden ${className ?? ""}`} style={{ gridTemplateColumns: `repeat(${colunas}, minmax(0, 1fr))` }}>
      {ladrilhos.map((l, i) => (
        <div key={i} className="relative aspect-square overflow-hidden" style={{ background: COR[l.fundo] }}>
          <span className="absolute rounded-full" style={{ left: l.circulo.x, top: l.circulo.y, width: l.circulo.d, height: l.circulo.d, background: COR[l.circulo.cor] }} />
        </div>
      ))}
    </div>
  );
}
```

- [ ] **Step 4: Reescrever `Logo.tsx`**

O símbolo (`docs/prototipo/Logo.dc.html:23`) com as cores por tom:

| tom | fundo | lupa | ouro |
|---|---|---|---|
| `cor` | `#0B6B3A` | `#FFFFFF` | sim |
| `claro` (sobre verde) | `#F6F4EE` | `#0A4D2E` | sim |
| `mono` | `currentColor` | `var(--color-cartao)` | não |

A palavra: `Busca` em `text-tinta-900` (`tom="claro"`: branco) e `<span>` `Concurso` em `text-acao` (`tom="claro"`: `text-ouro`). Variante `empilhado`: `flex-col items-center gap-[18px]` e a palavra a `tamanho * 0.38`. Variante `simbolo`: só o SVG. O SVG tem `aria-hidden` e o texto é real (leitor de tela lê "BuscaConcurso"). Apague o comentário antigo sobre documento e lente e escreva um curto sobre o azulejo de Athos Bulcão (texto de `Logo.dc.html:18`).

- [ ] **Step 5: Rodar os testes e conferir na tela**

Run: `pnpm vitest run src/components/marca && pnpm typecheck`
Expected: PASS. O cabeçalho atual já usa `<Logo tamanho={26} />`: o logo novo aparece em `http://192.168.15.100:3000`.

- [ ] **Step 6: Commit**

```bash
git add src/components/marca
git commit -m "feat: logo da lupa sobre o azulejo e o componente de azulejos"
```

---

### Task 5: Primitivos e o placeholder "Em breve"

**Files:**
- Modify: `src/components/ui/{Botao,Cartao,Etiqueta,Campo,Trilha,Paginacao,Secao}.tsx`, `src/lib/situacao.ts`
- Create: `src/components/ui/{Calendario,Abas,EmBreve}.tsx`, `src/lib/emBreve.ts`, `src/app/em-breve/[recurso]/page.tsx`
- Test: `src/lib/emBreve.test.ts`, `src/components/ui/Calendario.test.ts`, `src/lib/situacao.test.ts` (ajuste)

**Interfaces:**
- Consumes: `Icone` (Task 3), tokens (Task 2).
- Produces:
  - `Botao`/`BotaoLink` com `variante: "primario" | "chamada" | "secundario" | "contorno" | "fantasma"` e `tamanho: "sm" | "md" | "lg" | "xl"` = alturas 42/44/52/56, raio `rounded-controle` (11 px; `xl` usa 12 px), fonte 14/15/16/16 600 (`chamada` 700). Mantém as props de hoje (`href`, `icone?: NomeDoIcone`, `iconeDepois?: NomeDoIcone`).
  - `Cartao({ as, tom?: "neutro" | Tom, className, children })`: `bg-cartao rounded-cartao shadow-cartao`.
  - `Selo({ sigla, tamanho = 44 })`: `rounded-[12px] bg-anil-fundo text-anil-texto` com a fonte de 11 px 700 (12 px se `tamanho >= 44` e a sigla tiver até 4 letras).
  - `Etiqueta({ tom?: "neutro" | "verde" | "anil" | "ouro" | "urucum", icone?, children })`: pílula `h-[26px] px-[9px] rounded-full text-xs font-semibold`.
  - `Calendario({ iso, hoje, tamanho = "md" })`: `md` é 56×60 (faixa de 18 px) e `lg` é 64×72. O tom: `diasAte(iso, hoje) <= 0` é urucum, `=== 1` é ouro, senão verde.
  - `Abas({ rotulo, itens: { id, rotulo, href?, ativo }[], tamanho?: "md" | "sm" })`: trilho `bg-rebaixada p-1 rounded-[12px]`, aba ativa `bg-cartao shadow-aba`. Com `href` são links (`aria-current`); sem `href`, o componente vira cliente e usa `role="tab"` (Task 13).
  - `RECURSOS_EM_BREVE: Record<RecursoEmBreve, { titulo: string; frase: string }>`, com `type RecursoEmBreve = "salvos" | "areas" | "diario-oficial" | "como-lemos" | "acessibilidade" | "contato" | "alertas" | "alto-contraste" | "tamanho-da-fonte" | "lembrete"`, e `hrefEmBreve(r: RecursoEmBreve): string`.
  - `BotaoEmBreve({ recurso, className, children, "aria-label"? })` (cliente): no clique mostra `RECURSOS_EM_BREVE[recurso].titulo` num aviso `role="status" aria-live="polite"` fixo embaixo, por 4 s.

- [ ] **Step 1: Testes (falham)**

`src/lib/emBreve.test.ts`:
```ts
import { describe, expect, it } from "vitest";
import { hrefEmBreve, RECURSOS_EM_BREVE, recursoEmBreve } from "./emBreve";

describe("emBreve", () => {
  it("todo recurso tem título e frase sem travessão", () => {
    for (const [chave, { titulo, frase }] of Object.entries(RECURSOS_EM_BREVE)) {
      expect(titulo.length, chave).toBeGreaterThan(0);
      expect(frase.length, chave).toBeGreaterThan(0);
      expect(`${titulo}${frase}`).not.toContain("\u2014");
    }
  });

  it("o endereço é /em-breve/<recurso>", () => {
    expect(hrefEmBreve("diario-oficial")).toBe("/em-breve/diario-oficial");
  });

  it("recurso desconhecido é null", () => {
    expect(recursoEmBreve("banana")).toBeNull();
    expect(recursoEmBreve("salvos")).toBe("salvos");
  });
});
```
`src/components/ui/Calendario.test.ts`:
```ts
import { createElement } from "react";
import { renderToStaticMarkup } from "react-dom/server";
import { describe, expect, it } from "vitest";
import { Calendario, tomDoCalendario } from "./Calendario";

const HOJE = new Date(2026, 8, 24);

describe("Calendario", () => {
  it("hoje é urucum, amanhã é ouro, depois é verde", () => {
    expect(tomDoCalendario("2026-09-24", HOJE)).toBe("urucum");
    expect(tomDoCalendario("2026-09-25", HOJE)).toBe("ouro");
    expect(tomDoCalendario("2026-09-30", HOJE)).toBe("verde");
  });

  it("mostra mês abreviado em caixa alta e o dia", () => {
    const html = renderToStaticMarkup(createElement(Calendario, { iso: "2026-09-25", hoje: HOJE }));
    expect(html).toContain("SET");
    expect(html).toContain(">25<");
  });
});
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `pnpm vitest run src/lib/emBreve.test.ts src/components/ui/Calendario.test.ts`
Expected: FAIL, módulos ausentes.

- [ ] **Step 3: Implementar `emBreve.ts`**

```ts
export type RecursoEmBreve =
  | "salvos" | "areas" | "diario-oficial" | "como-lemos" | "acessibilidade"
  | "contato" | "alertas" | "alto-contraste" | "tamanho-da-fonte" | "lembrete";

export const RECURSOS_EM_BREVE: Record<RecursoEmBreve, { titulo: string; frase: string }> = {
  salvos: { titulo: "Concursos salvos", frase: "Guarde os concursos que interessam e veja todos num lugar só." },
  areas: { titulo: "Concursos por área", frase: "Tribunais, polícia, saúde e as outras áreas, cada uma com a sua lista." },
  "diario-oficial": { titulo: "Feed do Diário Oficial", frase: "Todo ato novo sobre concurso, do mais recente para o mais antigo." },
  "como-lemos": { titulo: "Como lemos os editais", frase: "O caminho do ato publicado até a página: coleta, leitura e conferência." },
  acessibilidade: { titulo: "Acessibilidade", frase: "O que fazemos para o site funcionar com leitor de tela, teclado e zoom." },
  contato: { titulo: "Contato", frase: "Um canal para falar com a equipe e apontar erro de leitura." },
  alertas: { titulo: "Alertas", frase: "Receba o edital no dia em que ele sair." },
  "alto-contraste": { titulo: "Alto contraste", frase: "Uma versão de cores com contraste máximo." },
  "tamanho-da-fonte": { titulo: "Tamanho da fonte", frase: "Aumentar e diminuir o texto do site inteiro." },
  lembrete: { titulo: "Lembretes", frase: "Um aviso na véspera do fim das inscrições." },
};

export function hrefEmBreve(recurso: RecursoEmBreve): string {
  return `/em-breve/${recurso}`;
}

export function recursoEmBreve(valor: string): RecursoEmBreve | null {
  return valor in RECURSOS_EM_BREVE ? (valor as RecursoEmBreve) : null;
}
```

- [ ] **Step 4: Implementar `Calendario.tsx`**

```tsx
import { diasAte } from "@/lib/formato";

const MESES = ["JAN", "FEV", "MAR", "ABR", "MAI", "JUN", "JUL", "AGO", "SET", "OUT", "NOV", "DEZ"];

export type TomDoCalendario = "urucum" | "ouro" | "verde";

export function tomDoCalendario(iso: string, hoje: Date): TomDoCalendario {
  const dias = diasAte(iso, hoje);
  return dias <= 0 ? "urucum" : dias === 1 ? "ouro" : "verde";
}

const ESTILO: Record<TomDoCalendario, { caixa: string; faixa: string; dia: string }> = {
  urucum: { caixa: "bg-urucum-fundo", faixa: "bg-urucum text-white", dia: "text-urucum-texto" },
  ouro: { caixa: "bg-ouro-fundo", faixa: "bg-ouro-faixa text-white", dia: "text-ouro-sinal-texto" },
  verde: { caixa: "bg-verde-fundo", faixa: "bg-acao text-acao-texto", dia: "text-verde-texto" },
};

export function Calendario({ iso, hoje, tamanho = "md" }: { iso: string; hoje: Date; tamanho?: "md" | "lg" }) {
  const [, mes, dia] = iso.split("-");
  const estilo = ESTILO[tomDoCalendario(iso, hoje)];
  const lg = tamanho === "lg";
  return (
    <div aria-hidden="true" className={`flex shrink-0 flex-col items-center overflow-hidden rounded-[12px] ${estilo.caixa} ${lg ? "h-[72px] w-16" : "h-[60px] w-14"}`}>
      <span className={`flex h-[18px] w-full items-center justify-center text-[10px] font-bold ${estilo.faixa}`}>{MESES[Number(mes) - 1]}</span>
      <span className={`font-titulo font-bold ${estilo.dia} ${lg ? "text-[30px] leading-[54px]" : "text-2xl leading-10"}`}>{Number(dia)}</span>
    </div>
  );
}
```
`diasAte` já existe em `src/lib/formato.ts:95`. Confira o sinal dele: hoje dá 0 e amanhã dá 1. Se o sinal for outro, ajuste a regra.

- [ ] **Step 5: Rodar e ver passar**

Run: `pnpm vitest run src/lib/emBreve.test.ts src/components/ui/Calendario.test.ts`
Expected: PASS.

- [ ] **Step 6: `EmBreve.tsx`, a página `/em-breve/[recurso]` e `Abas.tsx`**

`EmBreve.tsx` (`"use client"`): o estado `aberto` com `setTimeout` de 4 s. O aviso é `fixed bottom-6 left-1/2 -translate-x-1/2 rounded-[14px] bg-tinta-900 px-4 py-3 text-sm font-semibold text-cartao shadow-flutuante` e diz "Em breve: {titulo}".

`src/app/em-breve/[recurso]/page.tsx`: leia `node_modules/next/dist/docs/` sobre `params` assíncrono antes. O esqueleto:
```tsx
import type { Metadata } from "next";
import { notFound } from "next/navigation";
import { Azulejos, FAIXA_MARCA } from "@/components/marca/Azulejos";
import { BotaoLink } from "@/components/ui/Botao";
import { RECURSOS_EM_BREVE, recursoEmBreve } from "@/lib/emBreve";

export const metadata: Metadata = { robots: { index: false, follow: true } };

export default async function EmBreve({ params }: PageProps<"/em-breve/[recurso]">) {
  const recurso = recursoEmBreve((await params).recurso);
  if (!recurso) notFound();
  const { titulo, frase } = RECURSOS_EM_BREVE[recurso];
  return (
    <section className="mx-auto grid max-w-[1216px] gap-8 px-4 py-16 md:grid-cols-[1fr_440px] md:px-0">
      <div className="flex flex-col justify-center gap-4">
        <p className="text-[13px] font-bold tracking-[0.06em] text-acao">EM BREVE</p>
        <h1 className="font-titulo text-[40px] font-bold leading-[1.05] tracking-[-0.03em]">{titulo}</h1>
        <p className="max-w-[520px] text-[17px] leading-[1.55] text-tinta-600">{frase}</p>
        <BotaoLink href="/" variante="primario" tamanho="md" iconeDepois="seta" className="self-start">Voltar para o início</BotaoLink>
      </div>
      <Azulejos ladrilhos={FAIXA_MARCA} colunas={6} className="hidden rounded-painel md:grid" />
    </section>
  );
}
```
`Abas.tsx`: o markup de `docs/prototipo/Main.dc.html:195-200`. Os itens com `href` são `<Link>` com `aria-current="page"` na ativa.

- [ ] **Step 7: Reestilizar os primitivos existentes**

Para cada arquivo, as medidas vêm do protótipo:
- `Botao.tsx`: as variantes e alturas da seção Interfaces. `primario` como em `Main.dc.html:160` (`bg-acao text-acao-texto`), `chamada` como em `:69` (`bg-ouro text-ouro-texto font-bold`), `secundario` como em `:202` (`bg-rebaixada text-tinta-900 font-medium`), `contorno` como em `:49` (`shadow-[inset_0_0_0_1px_var(--color-contorno)]`), `fantasma` sem fundo. Mantém o anel de foco global.
- `Cartao.tsx`: `Cartao` como acima. `Selo` como acima. `BlocoDeNumeros` e `Numero` viram a caixa de fato de `Concurso.dc.html` (Task 12) e continuam exportados.
- `Etiqueta.tsx`: como na Interface. `Rotulo` vira o rótulo de seção (13 px, 700, `tracking-[0.06em]`, caixa alta, ícone de 16 px, cor do sinal).
- `Campo.tsx`/`Selecao`: altura 44, `rounded-controle`, `bg-cartao`, `shadow-[inset_0_0_0_1px_var(--color-contorno)]`, foco com o anel.
- `Trilha.tsx`: 14 px, `text-tinta-600`, separador `/` em `text-tinta-500`, o último degrau em `text-tinta-900 font-semibold`.
- `Paginacao.tsx`: botões `secundario` `sm`; a página atual em `bg-acao text-acao-texto`.
- `Secao.tsx`: o cabeçalho de seção de `Main.dc.html:128-131` (rótulo, `h2` de 40 px em `font-titulo` com `tracking-[-0.03em] leading-[1.05]` e link "Ver todos" com seta à direita). Props: `{ rotulo, icone, tom, titulo, apoio?, href?, hrefRotulo?, children }`.
- `situacao.ts` (`ESTILO_DO_TOM`): urgente para `bg-urucum-fundo text-urucum-texto`, previsto para `bg-ouro-fundo text-ouro-sinal-texto`, aberto para `bg-verde-fundo text-verde-texto`, encerrado para `bg-rebaixada text-tinta-600`. Ajuste o `situacao.test.ts` só se ele fixar nomes de classe.

- [ ] **Step 8: Verificar e fazer o commit**

Run: `pnpm verificar`
Expected: PASS. Abra `http://192.168.15.100:3000/em-breve/salvos` (a página nova) e `/em-breve/banana` (404).
```bash
git add src/components/ui src/lib/emBreve.ts src/lib/emBreve.test.ts src/lib/situacao.ts src/lib/situacao.test.ts src/app/em-breve
git commit -m "feat: primitivos no visual novo, calendário, abas e o placeholder em breve"
```

---

### Task 6: Casca: barra utilitária, nav e rodapé

**Files:**
- Create: `src/components/layout/BarraUtilitaria.tsx`, `src/components/layout/NavPrincipal.tsx`
- Modify: `src/components/layout/{Cabecalho,Rodape,SeletorDeTema,MenuConta}.tsx`, `src/app/layout.tsx`, `src/components/busca/BarraBusca.tsx` (só o visual da versão do cabeçalho)
- Test: `src/components/layout/NavPrincipal.test.ts`

**Interfaces:**
- Consumes: `Logo`, `Icone`, `BotaoEmBreve`, `hrefEmBreve`, `BarraBuscaDoCabecalho`, `MenuConta`, `SeletorDeTema`, `cargosEscolhidos()`.
- Produces:
  - `ITENS_DA_NAV: { rotulo: string; icone: NomeDoIcone; href: string; ativo: (caminho: string, busca: URLSearchParams) => boolean }[]`
  - `BarraUtilitaria({ atualizadoEm }: { atualizadoEm: string | null })`
  - `Cabecalho({ dimensoes, atualizadoEm })`: o `layout.tsx` passa `atualizadoEm = dataCurta(hojeEmSaoPaulo())` quando a origem é `"api"`, e `null` quando não.

- [ ] **Step 1: Teste da regra de aba ativa (falha)**

```ts
import { describe, expect, it } from "vitest";
import { ITENS_DA_NAV } from "./NavPrincipal";

const ativo = (rotulo: string, caminho: string, busca = "") =>
  ITENS_DA_NAV.find((item) => item.rotulo === rotulo)!.ativo(caminho, new URLSearchParams(busca));

describe("ITENS_DA_NAV", () => {
  it("Abertos acende na lista com situação aberta", () => {
    expect(ativo("Abertos", "/concursos", "situacao=abertas")).toBe(true);
    expect(ativo("Abertos", "/concursos", "situacao=previstos")).toBe(false);
  });

  it("Previstos acende na lista com situação prevista", () => {
    expect(ativo("Previstos", "/concursos", "situacao=previstos")).toBe(true);
  });

  it("Diário Oficial e Áreas acendem na página em breve deles", () => {
    expect(ativo("Diário Oficial", "/em-breve/diario-oficial")).toBe(true);
    expect(ativo("Áreas", "/em-breve/areas")).toBe(true);
  });

  it("nenhuma acende na home", () => {
    expect(ITENS_DA_NAV.some((item) => item.ativo("/", new URLSearchParams()))).toBe(false);
  });
});
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `pnpm vitest run src/components/layout/NavPrincipal.test.ts`
Expected: FAIL, módulo ausente.

- [ ] **Step 3: `NavPrincipal.tsx`** (`"use client"`, `usePathname` e `useSearchParams`)

```ts
export const ITENS_DA_NAV = [
  { rotulo: "Abertos", icone: "aberto", href: "/concursos?situacao=abertas",
    ativo: (c, b) => c === "/concursos" && b.get("situacao") === "abertas" },
  { rotulo: "Previstos", icone: "previsto", href: "/concursos?situacao=previstos",
    ativo: (c, b) => c === "/concursos" && b.get("situacao") === "previstos" },
  { rotulo: "Diário Oficial", icone: "diario", href: hrefEmBreve("diario-oficial"),
    ativo: (c) => c === hrefEmBreve("diario-oficial") },
  { rotulo: "Áreas", icone: "areas", href: hrefEmBreve("areas"),
    ativo: (c) => c === hrefEmBreve("areas") },
  { rotulo: "Estados", icone: "estados", href: "/#estados", ativo: () => false },
] satisfies ItemDaNav[];
```
O markup de `docs/prototipo/Main.dc.html:39-45`: cada item com `h-10 px-3 rounded-[10px] text-[15px] gap-2`, ícone de 18 px, ativo em `bg-verde-fundo text-verde-texto font-semibold`, inativo em `text-tinta-900 font-medium hover:bg-rebaixada`, e `aria-current="page"` no ativo. `useSearchParams` exige `<Suspense>` em volta no `Cabecalho` (confira em `node_modules/next/dist/docs/`), com a nav sem estado ativo como fallback.

- [ ] **Step 4: Rodar e ver passar**

Run: `pnpm vitest run src/components/layout/NavPrincipal.test.ts`
Expected: PASS.

- [ ] **Step 5: `BarraUtilitaria.tsx`**

Markup de `Main.dc.html:24-31`: `hidden md:flex h-9 bg-utilitaria text-utilitaria-texto text-[13px] px-[112px] justify-between`. As três faixinhas de 14×4 (`#2FA35F`, `#F2C230`, `#5C7FD6`, fixas, `aria-hidden`). O texto "Dados públicos, lidos do Diário Oficial e das bancas" mais ` · Atualizado em {atualizadoEm}` quando não é `null`. À direita: `BotaoEmBreve recurso="alto-contraste"` (ícone `contraste` de 15 px e o texto), `BotaoEmBreve recurso="tamanho-da-fonte"` duas vezes (`A−` com `aria-label="Diminuir fonte"` e `A+` com `aria-label="Aumentar fonte"`), `<SeletorDeTema compacto />` e o link "Como lemos os editais" para `hrefEmBreve("como-lemos")`. O `A−` usa o sinal de menos U+2212, que não é travessão.

`SeletorDeTema`: nova prop `compacto`, que desenha os três botões de ícone de 15 px (`sol`, `lua`, `monitor`) em `text-utilitaria-texto` com `aria-pressed`, sem rótulo visível e com `aria-label` ("Tema claro", "Tema escuro", "Tema do sistema").

- [ ] **Step 6: `Cabecalho.tsx`**

Estrutura: `<header>` com `<BarraUtilitaria>` e depois a `<nav aria-label="Principal">` de `Main.dc.html:34-51` (`h-[76px] bg-cartao border-b border-linha px-4 md:px-[112px] gap-10`). Dentro dela:
- `Link href="/"` com `<Logo tamanho={36} />` (`aria-label="BuscaConcurso, página inicial"`)
- `<NavPrincipal />` (`hidden lg:flex flex-1`)
- a busca compacta (`BarraBuscaDoCabecalho`) só fora da home: um componente cliente `SoForaDaHome` que usa `usePathname() !== "/"`. Ela ocupa o lugar das abas até `xl` e fica ao lado delas em `xl`. Reestilize a cápsula da `BarraBusca` para `h-11 rounded-controle bg-rebaixada` com `kbd` `/` (`Main.dc.html:64`), sem mudar a lógica.
- à direita: `BotaoEmBreve recurso="salvos"` (ícone `salvar`, 44×44, `aria-label="Salvos"`), `BotaoEmBreve recurso="alertas"` (ícone `alerta` mais a bolinha urucum de `Main.dc.html:48`, `aria-label="Meus alertas"`) e `MenuConta`.
- `MenuConta` deslogado: `BotaoLink variante="contorno" tamanho="md" icone="entrar"` "Entrar". Logado: o menu atual com o gatilho no mesmo formato.
- Celular (`< lg`): logo, busca, e o botão `menu` que abre a `Gaveta` existente com os `ITENS_DA_NAV` e o seletor de tema. Mantenha o comportamento de fechar a gaveta ao navegar (spec de 24/09, pedido 6).

- [ ] **Step 7: `Rodape.tsx`**

Markup de `Main.dc.html:411-436` (leia o bloco inteiro): `bg-cartao border-t border-linha`, container `px-4 md:px-[112px] py-14`. Colunas: marca e frase; BUSCAR (Abertos, Previstos, Diário Oficial, Por área); CARGOS MAIS BUSCADOS (os 8 primeiros de `cargosEscolhidos()`, com o mesmo `try/catch` e o log `[rodape]` que o rodapé já tem); SOBRE (Como lemos os editais, Design system para `/estilo`, Acessibilidade, Contato). A linha final "© 2026 BuscaConcurso · Acervo atualizado em {data}" e o crédito `LogoGvTechLab` à direita. No celular: marca e a linha final (`Mobile.dc.html`, fim do arquivo) e as colunas empilhadas.

- [ ] **Step 8: Verificar e fazer o commit**

Run: `pnpm verificar`
Expected: PASS. Em `http://192.168.15.100:3000` confira a barra utilitária, a nav de 76 px com as abas, o "Entrar" com contorno, o rodapé novo e a gaveta no celular (DevTools a 390 px). Clicar em "Salvos" mostra "Em breve: Concursos salvos".
```bash
git add src/components/layout src/components/busca/BarraBusca.tsx src/app/layout.tsx
git commit -m "feat: barra utilitária, nav e rodapé do visual novo"
```

---

### Task 7: Dados da home: áreas, mapa por UF e facetas completas

**Files:**
- Create: `src/lib/areas.ts`, `src/lib/mapaUf.ts`
- Modify: `src/lib/concursos.ts:535-600` (`facetas`)
- Test: `src/lib/areas.test.ts`, `src/lib/mapaUf.test.ts`, `src/lib/concursos.test.ts`

**Interfaces:**
- Produces:
  - `AREAS: Area[]` com `interface Area { nome: string; apoio: string; icone: NomeDoIcone; tom: "verde" | "anil" | "ouro" | "urucum"; termo: string }` e `hrefDaArea(area: Area): string` (usa `caminhoDaBusca(area.termo)` de `parametros.ts`)
  - `POSICAO_UF: Record<Uf, { coluna: number; linha: number }>`
  - `type NivelDoMapa = 0 | 1 | 2 | 3 | 4 | 5 | 6 | 7`, `nivelDoMapa(total: number, maximo: number): NivelDoMapa`
  - `ESTILO_DO_NIVEL: Record<NivelDoMapa, { fundo: string; texto: string }>` (hex, porque é visualização de dado com a mesma escala nos dois temas; o nível 0 usa os tokens `bg-rebaixada text-tinta-500`)
  - `facetas(hoje, { ufs?: number } = {})`: `ufs` é o limite de UFs (padrão 12, como hoje). A home passa `ufs: 27`.

- [ ] **Step 1: Testes (falham)**

`src/lib/areas.test.ts`:
```ts
import { describe, expect, it } from "vitest";
import { AREAS, hrefDaArea } from "./areas";

describe("AREAS", () => {
  it("são as 12 do protótipo, na ordem", () => {
    expect(AREAS.map((a) => a.nome)).toEqual([
      "Tribunais", "Polícia e segurança", "Educação", "Saúde", "Fiscal e controle",
      "Bancos e estatais", "Forças Armadas", "Prefeituras", "Conselhos", "Tecnologia",
      "Administrativo", "Ambiente e agro",
    ]);
  });

  it("cada área leva à busca pelo termo dela", () => {
    expect(hrefDaArea(AREAS[0])).toBe("/busca/tribunal");
    for (const area of AREAS) expect(hrefDaArea(area)).toMatch(/^\/busca\/[a-z0-9-]+$/);
  });
});
```
`src/lib/mapaUf.test.ts`:
```ts
import { describe, expect, it } from "vitest";
import { UFS } from "./dominio";
import { nivelDoMapa, POSICAO_UF } from "./mapaUf";

describe("mapa por UF", () => {
  it("tem posição para as 27 UFs e nenhuma célula repetida", () => {
    expect(Object.keys(POSICAO_UF).sort()).toEqual([...UFS].sort());
    const celulas = Object.values(POSICAO_UF).map(({ coluna, linha }) => `${coluna},${linha}`);
    expect(new Set(celulas).size).toBe(27);
  });

  it("reproduz a escala do protótipo (máximo 31)", () => {
    expect(nivelDoMapa(0, 31)).toBe(0);
    expect(nivelDoMapa(1, 31)).toBe(1);
    expect(nivelDoMapa(2, 31)).toBe(2);
    expect(nivelDoMapa(4, 31)).toBe(3);
    expect(nivelDoMapa(6, 31)).toBe(4);
    expect(nivelDoMapa(7, 31)).toBe(5);
    expect(nivelDoMapa(9, 31)).toBe(5);
    expect(nivelDoMapa(18, 31)).toBe(6);
    expect(nivelDoMapa(31, 31)).toBe(7);
  });

  it("acervo vazio não divide por zero", () => {
    expect(nivelDoMapa(0, 0)).toBe(0);
  });
});
```
E em `concursos.test.ts`:
```ts
describe("facetas", () => {
  it("devolve todas as UFs com aberto quando o limite é 27", async () => {
    const { ufs } = await facetas(HOJE, { ufs: 27 });
    const padrao = await facetas(HOJE);
    expect(ufs.length).toBeGreaterThanOrEqual(padrao.ufs.length);
    expect(padrao.ufs.length).toBeLessThanOrEqual(12);
  });
});
```
(importe `facetas` no topo do arquivo).

- [ ] **Step 2: Rodar e ver falhar**

Run: `pnpm vitest run src/lib/areas.test.ts src/lib/mapaUf.test.ts src/lib/concursos.test.ts`
Expected: FAIL.

- [ ] **Step 3: `areas.ts`**

Nome e apoio de `docs/prototipo/Main.dc.html:133-144`, o ícone da Task 3 e o tom do fundo do ícone:
```ts
export const AREAS: Area[] = [
  { nome: "Tribunais", apoio: "TJ, TRT, TRF, TRE", icone: "tribunais", tom: "verde", termo: "tribunal" },
  { nome: "Polícia e segurança", apoio: "PF, PRF, PM, PC", icone: "policia", tom: "anil", termo: "policia" },
  { nome: "Educação", apoio: "Universidades e IFs", icone: "educacao", tom: "ouro", termo: "professor" },
  { nome: "Saúde", apoio: "Hospitais e SUS", icone: "saude", tom: "urucum", termo: "saude" },
  { nome: "Fiscal e controle", apoio: "Receita, TCU, CGU", icone: "fiscal", tom: "verde", termo: "auditor" },
  { nome: "Bancos e estatais", apoio: "BB, Caixa, BNDES", icone: "estatais", tom: "anil", termo: "banco" },
  { nome: "Forças Armadas", apoio: "Marinha, Exército, FAB", icone: "forcas", tom: "anil", termo: "militar" },
  { nome: "Prefeituras", apoio: "Municípios do país", icone: "prefeituras", tom: "ouro", termo: "prefeitura" },
  { nome: "Conselhos", apoio: "CRA, COREN, CREA", icone: "conselhos", tom: "verde", termo: "conselho" },
  { nome: "Tecnologia", apoio: "Analista de TI e dados", icone: "tecnologia", tom: "anil", termo: "tecnologia" },
  { nome: "Administrativo", apoio: "Técnico e assistente", icone: "administrativo", tom: "urucum", termo: "administrativo" },
  { nome: "Ambiente e agro", apoio: "IBAMA, ICMBio, CONAB", icone: "ambiente", tom: "verde", termo: "ambiental" },
];
```
Confira cada termo contra o acervo local antes de fazer o commit, porque um termo sem resultado leva a uma página vazia:
```bash
for t in tribunal policia professor saude auditor banco militar prefeitura conselho tecnologia administrativo ambiental; do printf "%s " $t; curl -s "http://127.0.0.1:3000/busca/$t" | grep -oE '[0-9.]+ (concursos?|resultados?)' | head -1; done
```
Se um termo trouxer zero, troque pelo sinônimo mais frequente (por exemplo `ibama`) e registre isso num comentário.

- [ ] **Step 4: `mapaUf.ts`**

As posições saem de `Main.dc.html:277-300`, e as cores e os níveis também:
```ts
export const POSICAO_UF: Record<Uf, { coluna: number; linha: number }> = {
  RR: { coluna: 3, linha: 1 }, AP: { coluna: 5, linha: 1 },
  AM: { coluna: 2, linha: 2 }, PA: { coluna: 4, linha: 2 }, MA: { coluna: 5, linha: 2 }, CE: { coluna: 6, linha: 2 }, RN: { coluna: 7, linha: 2 },
  AC: { coluna: 1, linha: 3 }, RO: { coluna: 2, linha: 3 }, MT: { coluna: 3, linha: 3 }, TO: { coluna: 4, linha: 3 }, PI: { coluna: 5, linha: 3 }, PE: { coluna: 6, linha: 3 }, PB: { coluna: 7, linha: 3 },
  MS: { coluna: 3, linha: 4 }, GO: { coluna: 4, linha: 4 }, DF: { coluna: 5, linha: 4 }, BA: { coluna: 6, linha: 4 }, AL: { coluna: 7, linha: 4 },
  SP: { coluna: 4, linha: 5 }, MG: { coluna: 5, linha: 5 }, ES: { coluna: 6, linha: 5 }, SE: { coluna: 7, linha: 5 },
  PR: { coluna: 3, linha: 6 }, RJ: { coluna: 5, linha: 6 },
  SC: { coluna: 3, linha: 7 },
  RS: { coluna: 3, linha: 8 },
};

/** Faixas medidas sobre o protótipo, onde o máximo é 31 (MG). */
export function nivelDoMapa(total: number, maximo: number): NivelDoMapa {
  if (total <= 0 || maximo <= 0) return 0;
  const razao = total / maximo;
  if (razao >= 1) return 7;
  if (razao >= 0.5) return 6;
  if (razao >= 0.22) return 5;
  if (razao >= 0.16) return 4;
  if (razao >= 0.1) return 3;
  if (razao >= 0.05) return 2;
  return 1;
}

export const ESTILO_DO_NIVEL: Record<Exclude<NivelDoMapa, 0>, { fundo: string; texto: string }> = {
  1: { fundo: "#E1F0E6", texto: "#0A4D2E" },
  2: { fundo: "#C9E4D2", texto: "#0A4D2E" },
  3: { fundo: "#8CC5A0", texto: "#0A4D2E" },
  4: { fundo: "#5FAE7E", texto: "#FFFFFF" },
  5: { fundo: "#3E9A63", texto: "#FFFFFF" },
  6: { fundo: "#0B6B3A", texto: "#FFFFFF" },
  7: { fundo: "#0A4D2E", texto: "#FFFFFF" },
};
```
(Ajuste o tipo da Interface para `Record<Exclude<NivelDoMapa, 0>, ...>`: o nível 0 usa tokens.) O branco sobre `#5FAE7E` fica abaixo de 4.5:1. Como é número de 11 a 12 px, use `texto: "#0A4D2E"` no nível 4 e confira o contraste com `razaoDeContraste` num caso a mais em `mapaUf.test.ts`: `expect(razaoDeContraste(hex(texto), hex(fundo))).toBeGreaterThanOrEqual(4.5)` para todo nível. Importe `lerCor` e `razaoDeContraste` de `./contraste`.

- [ ] **Step 5: `facetas` com limite**

Em `src/lib/concursos.ts`, troque a assinatura para `facetas(hoje: Date = new Date(), { ufs: limiteUfs = 12 }: { ufs?: number } = {})` e use `maisFrequentes(porUf, limiteUfs)`. Os outros chamadores não mudam.

- [ ] **Step 6: Rodar e ver passar, depois fazer o commit**

Run: `pnpm vitest run src/lib && pnpm typecheck`
Expected: PASS.
```bash
git add src/lib/areas.ts src/lib/areas.test.ts src/lib/mapaUf.ts src/lib/mapaUf.test.ts src/lib/concursos.ts src/lib/concursos.test.ts
git commit -m "feat: áreas, grade do mapa por UF e facetas com todas as UFs"
```

---

### Task 8: Home, parte 1: hero, mosaico e números

**Files:**
- Modify: `src/components/home/Hero.tsx` (reescrita)
- Create: `src/components/home/Mosaico.tsx`, `src/components/home/Numeros.tsx`
- Test: `src/components/home/Hero.test.ts`

**Interfaces:**
- Consumes: `Azulejos`/`MOSAICO_HERO`, `Icone`, `Selo`, `Botao`, `destinoDoFormulario`, `ufLembrada`, `ConcursoResumo`, `moeda`, `dataCurta`, `vagasTexto`, `nomeCurtoDoOrgao`.
- Produces:
  - `Hero({ totalAbertos, ufs, cargos, destaque, novoAto }: { totalAbertos: number; ufs: LinkDeFaceta[]; cargos: CargoMedido[]; destaque: ConcursoResumo | null; novoAto: ConcursoResumo | null })`
  - `Numeros({ totalAbertos, vagasPrevistas, atosLidos }: { totalAbertos: number; vagasPrevistas: number | null; atosLidos: number })`

- [ ] **Step 1: Teste (falha)**

```ts
import { createElement } from "react";
import { renderToStaticMarkup } from "react-dom/server";
import { describe, expect, it } from "vitest";
import { Hero } from "./Hero";
import { Numeros } from "./Numeros";
import { CONCURSOS } from "@/mocks/concursos";

describe("Hero", () => {
  it("mostra a contagem de abertos e o título do protótipo", () => {
    const html = renderToStaticMarkup(createElement(Hero, { totalAbertos: 113, ufs: [], cargos: [], destaque: CONCURSOS[0], novoAto: null }));
    expect(html).toContain("113");
    expect(html).toContain("Encontre seu concurso.");
    expect(html).toContain("Direto do edital.");
  });

  it("sem concurso de destaque não desenha o cartão flutuante", () => {
    const html = renderToStaticMarkup(createElement(Hero, { totalAbertos: 0, ufs: [], cargos: [], destaque: null, novoAto: null }));
    expect(html).not.toContain("Conferido no edital original");
  });
});

describe("Numeros", () => {
  it("formata com separador de milhar e esconde vagas previstas sem dado", () => {
    const html = renderToStaticMarkup(createElement(Numeros, { totalAbertos: 113, vagasPrevistas: null, atosLidos: 5016 }));
    expect(html).toContain("5.016");
    expect(html).not.toContain("vagas nos 4 maiores previstos");
  });
});
```
Se `Hero` virar cliente por causa da busca, deixe a busca num filho cliente (`BuscaDoHero`) e mantenha `Hero` como server component, para o teste renderizar sem hooks.

- [ ] **Step 2: Rodar e ver falhar**

Run: `pnpm vitest run src/components/home/Hero.test.ts`
Expected: FAIL.

- [ ] **Step 3: Implementar a partir do protótipo**

- `Hero.tsx`: `docs/prototipo/Main.dc.html:54-79` (desktop) e o começo de `Mobile.dc.html` (celular). `section` `bg-faixa text-white`, `min-h-[620px]` no desktop, `px-4 md:px-[112px] gap-16`. Coluna de texto `max-w-[700px]` com `gap-6`: a pílula (`:56`) com o número em ouro; o `h1` de 68 px (`md`) e 40 px (celular, conforme `Mobile.dc.html`), `leading-[1.02] tracking-[-0.035em]`, com a segunda linha `text-ouro`; o parágrafo de 19 px `text-faixa-texto`.
- `BuscaDoHero` (cliente): o `form` de `:60-70`, com `role="search"`, rótulo `sr-only` "Cargo, órgão ou banca", `input` `name="q"`, `select` de UF estilizado como o botão "Todo o Brasil" (opções das 27 UFs mais "Todo o Brasil"; valor inicial de `ufLembrada()` via `useSyncExternalStore`, como já faz hoje), `select` de escolaridade idem, e o botão `chamada` `xl` "Buscar" com a seta. No envio: `router.push(destinoDoFormulario(q, uf))` mais `&escolaridade=` quando escolhida. O atalho `/` foca o campo quando nenhum campo está focado (reaproveite o que `BarraBusca.tsx` já faz, extraindo para `useAtalhoDeBusca`). No celular, `Mobile.dc.html` empilha: campo, uma linha com UF e Escolaridade e o botão de largura total "Buscar concursos".
- Os chips de `:72-77` são links: "Encerram esta semana" para `/concursos?situacao=abertas` (a ordem padrão já é `encerrando`), "Acima de R$ 10 mil" para `/concursos?situacao=abertas&salarioMin=10000`, "Nível médio" para `/concursos?situacao=abertas&escolaridade=medio` (confira o valor exato em `ESCOLARIDADES`) e "Perto de mim", um link cliente que usa `ufLembrada()` para montar `?uf=` e, sem UF, `/concursos?situacao=abertas` com `#localizacao`, reaproveitando o que o `Hero` atual faz com `localizacao.ts` (leia o `Hero.tsx` antigo antes de apagar).
- `Mosaico.tsx`: `:81-113`. `hidden lg:flex`, `Azulejos` 448×448 com `rounded-[28px]` e `ml-10`. O cartão flutuante (`:102-110`) com `destaque`: `Selo`, título do cargo (`destaque.nomesDeCargo[0] ?? tituloSemOrgao(...)`), linha "Edital · cidade", as três caixas (salário `moeda(salarioAte)` ou "a definir", vagas `vagas ?? "a definir"`, ATÉ `dataCurta(inscricoesAte)` em urucum), e "Conferido no edital original". O cartão inteiro é um `Link` para `/concursos/{slug}`. A pílula (`:112`) com `novoAto`: "Novo ato no DOU · {nomeCurtoDoOrgao}", link para o concurso.
- `Numeros.tsx`: `:119-124`. `section` `relative -mt-14 mx-4 md:mx-[112px] rounded-[20px] bg-cartao shadow-numeros grid md:grid-cols-4`, com divisores `border-linha-fraca`. No celular, `Mobile.dc.html` mostra só dois números lado a lado ("abertos" e "vagas previstas"). Números em `font-titulo text-[32px]` com `numero()`. `vagasPrevistas === null` omite a segunda caixa.

- [ ] **Step 4: Rodar, conferir e fazer o commit**

Run: `pnpm vitest run src/components/home && pnpm typecheck`
Expected: PASS. A home ainda usa o `Hero` antigo com props antigas: ajuste a chamada em `src/app/page.tsx` agora (o `destaque` é `destaques.encerrando[0] ?? destaques.abertos[0] ?? null` e o `novoAto` é `destaques.atualizados[0] ?? null`). Monte `<Numeros>` logo abaixo com `vagasPrevistas = somaOuNull(destaques.previstos.map(c => c.vagas))` (null se todos forem null) e `atosLidos = aviso?.total ?? dimensoes.total`. Compare `http://192.168.15.100:3000` a 1440 e 390 com `Main.dc.html` e `Mobile.dc.html`.
```bash
git add src/components/home src/app/page.tsx
git commit -m "feat: hero da home com busca, mosaico de azulejos e números"
```

---

### Task 9: Home, parte 2: áreas, encerram, abertos, estados, previstos, DOU, como funciona e alerta

**Files:**
- Create: `src/components/home/{Areas,EncerramSemana,TabelaAbertos,PorEstado,VemAiEDou,ComoFunciona}.tsx`
- Modify: `src/components/home/BlocoAlerta.tsx`, `src/app/page.tsx`
- Delete: `src/components/home/{BlocosSeo,Ilustracao,Secao}.tsx`
- Test: `src/components/home/Home.test.ts`

**Interfaces:**
- Consumes: `Secao` (ui, Task 5), `Calendario`, `Abas`, `Selo`, `Etiqueta`, `BotaoEmBreve`, `AREAS`/`hrefDaArea`, `POSICAO_UF`/`nivelDoMapa`/`ESTILO_DO_NIVEL`, `facetas(hoje, { ufs: 27 })`, `acervoIncompletoEmPartes`, `textoDoAto`, `ROTULO_ESCOLARIDADE`.
- Produces: `Areas()`, `EncerramSemana({ concursos, hoje })`, `TabelaAbertos({ concursos, total, hoje })`, `PorEstado({ ufs, orgaos, bancas })`, `VemAiEDou({ previstos, atualizados })`, `ComoFunciona({ aviso })`, `BlocoAlerta({ totalAbertos })`.

- [ ] **Step 1: Teste do render da home com lista vazia e com mock (falha)**

```ts
import { createElement } from "react";
import { renderToStaticMarkup } from "react-dom/server";
import { describe, expect, it } from "vitest";
import { EncerramSemana } from "./EncerramSemana";
import { PorEstado } from "./PorEstado";
import { TabelaAbertos } from "./TabelaAbertos";
import { ComoFunciona } from "./ComoFunciona";
import { CONCURSOS } from "@/mocks/concursos";

const HOJE = new Date(2026, 8, 24);

describe("blocos da home", () => {
  it("encerram esta semana some sem concurso", () => {
    expect(renderToStaticMarkup(createElement(EncerramSemana, { concursos: [], hoje: HOJE }))).toBe("");
  });

  it("a tabela mostra o total e leva para a lista completa", () => {
    const html = renderToStaticMarkup(createElement(TabelaAbertos, { concursos: CONCURSOS.slice(0, 6), total: 113, hoje: HOJE }));
    expect(html).toContain("113 concursos abertos agora");
    expect(html).toContain('href="/concursos?situacao=abertas"');
  });

  it("o mapa desenha as 27 UFs mesmo sem aberto nenhum", () => {
    const html = renderToStaticMarkup(createElement(PorEstado, { ufs: [], orgaos: [], bancas: [] }));
    expect(html.match(/data-uf=/g)).toHaveLength(27);
    expect(html).toContain('id="estados"');
  });

  it("como funciona sem aviso não fala em atos fora da lista", () => {
    const html = renderToStaticMarkup(createElement(ComoFunciona, { aviso: null }));
    expect(html).toContain("Da fonte oficial para a sua tela");
    expect(html).not.toContain("ficaram fora");
  });
});
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `pnpm vitest run src/components/home/Home.test.ts`
Expected: FAIL.

- [ ] **Step 3: Implementar os blocos a partir do protótipo**

Todos dentro do container `px-4 md:px-[112px]` com `mt-24` entre seções (96 px, `Main.dc.html:127`), no celular `mt-12`. O cabeçalho de cada um usa o `Secao` da Task 5.
- `Areas.tsx`: `Main.dc.html:127-146`. Grade `grid-cols-2 sm:grid-cols-3 lg:grid-cols-6 gap-3`. Cada cartão é `Link` `h-[152px] p-5 rounded-cartao bg-cartao shadow-cartao flex-col justify-between`, com ícone de 26 px em caixa de 48 px `rounded-[14px] bg-{tom}-fundo text-{tom}-texto`. "Todas as áreas" leva para `hrefEmBreve("areas")`. No celular, `Mobile.dc.html` mostra 6 áreas em chips roláveis horizontalmente (`overflow-x-auto` com `snap-x` e um `min-w-0` no pai para não vazar), com "Ver todas" à direita.
- `EncerramSemana.tsx`: `:149-185`. Retorna `null` com lista vazia. O rótulo é "ÚLTIMA CHAMADA" em urucum com o ícone `prazo`. Os 4 cartões (`:156-161`): `Calendario`, a linha de urgência ("Encerra hoje" em `text-urucum`, "Encerra amanhã" em `text-ouro-sinal-texto`, senão "Encerra em N dias" em `text-tinta-600`), "{sigla} · {edital}" (reaproveite `tituloComOrgao` e `tituloDoAto`), o nome do órgão, duas metas (a banca com o ícone `banca` se houver, senão vagas com `pessoa`; e escolaridade ou UF), o `BotaoLink` `primario` `sm` "Ver edital" com `externo` para `/concursos/{slug}` e o `BotaoEmBreve recurso="salvos"` de 42×42 `bg-rebaixada` com `aria-label="Salvar"`. No celular (`Mobile.dc.html`) os cartões ficam horizontais e compactos, sem os botões, e o cartão inteiro vira link.
- `TabelaAbertos.tsx`: `:188-270` (leia inteiro). O `h2` é "{total} concursos abertos agora". Cartão `rounded-[20px] shadow-tabela overflow-hidden`. A barra de controle de 72 px com `Abas` (Todos {total}, Superior, Médio, Fundamental, com `href` para `/concursos?situacao=abertas&escolaridade=...`), "Ordenar: prazo mais curto" como `BotaoLink secundario` para `/concursos?situacao=abertas` e "Filtros" para o mesmo destino. O cabeçalho `bg-pagina` de 44 px com as colunas `grid-cols-[2.6fr_1.2fr_0.9fr_1fr_1.2fr_150px]`. Linhas de 88 px. Na coluna "Inscrições até", `dataCurta` e abaixo o prazo relativo (`prazoRelativo`) em urucum quando falta até 1 dia. A última coluna tem "Ver" (`BotaoLink contorno sm`). O rodapé traz "Mostrando {n} de {total} · ordenados pelo prazo" e "Ver todos os {total}". Em `< lg` vira lista de cartões como `Mobile.dc.html` ("Abertos agora"): selo, cargo, "Edital · cidade" e três etiquetas (salário, vagas, até). Use `<table>` de verdade no desktop (`<caption class="sr-only">`), e no celular uma `<ul>`: dois markups com `hidden lg:table` e `lg:hidden`.
- `PorEstado.tsx`: `:271-335`. `section id="estados"` com `scroll-mt-24`. Cartão de 620 px com a grade `grid-cols-[repeat(7,52px)] grid-rows-[repeat(8,52px)] gap-1.5`. Cada UF é um `Link href="/concursos?uf=XX"` com `data-uf`, posicionado por `style={{ gridColumn, gridRow }}`, cor por `nivelDoMapa(total, maximo)` e `ESTILO_DO_NIVEL`. O nível 7 leva `shadow-[0_0_0_3px_var(--color-ouro)]`. `aria-label="{Nome da UF}: {n} abertos"`. Legenda de 5 cores de 0 ao máximo e o link "Usar minha localização" (o mesmo "perto de mim" do hero). Ao lado, dois cartões: "ÓRGÃOS COM MAIS VAGAS ABERTAS" (os 6 primeiros de `orgaos`, número em `bg-verde-fundo text-verde-texto`) e "POR BANCA" (os 4 primeiros de `bancas`, número em `bg-anil-fundo text-anil-texto`). No celular o bloco não aparece (`hidden md:block`, como no protótipo mobile), mas os links de UF continuam no rodapé, via `BlocosSeo` removido: adicione no rodapé mobile uma linha "Por estado" com as 8 UFs de mais abertos. Assim o SEO dos links de UF não some.
- `VemAiEDou.tsx`: `:336-358`. Duas colunas `lg:grid-cols-2 gap-4`. "Vem aí": 4 linhas com o número de vagas em `font-titulo text-[28px]` ("vagas" ou "vagas + CR" quando `cadastroReserva`), sigla e edital, o nome e a escolaridade, e o `BotaoEmBreve recurso="alertas"` "Avisar". "Saiu no DOU": 4 linhas com o ícone por status (`homologado`, `previsto`, `aberto`), "{sigla} · {edital}", `textoDoAto(ultimoAto, titulo)` e a data `dataCurta`. "Ver feed" leva para `hrefEmBreve("diario-oficial")`. Some no celular.
- `ComoFunciona.tsx`: `:359-379`. Os três passos (01, 02 e 03 em `font-titulo` ouro sobre `bg-faixa`, texto de `:359-379` literal). A faixa de baixo só aparece com `aviso`: "{semDado} atos dos {total} lidos ficaram fora desta lista, e dizemos por quê." (sem travessão; o protótipo tem um, que vira vírgula) e as caixas das partes de `acervoIncompletoEmPartes(aviso)` (número mais rótulo de cada parte). Some no celular.
- `BlocoAlerta.tsx`: `:380-410` e o fim de `Mobile.dc.html`. Leia o componente atual primeiro: se ele posta para a API, mantenha. Se é visual, o `form` chama `BotaoEmBreve recurso="alertas"` no submit (em `onSubmit`, com `preventDefault`). Fundo `bg-faixa`, título em Bricolage, campo branco, botão `chamada` e as três garantias com `check`. Remova o export `AcervoIncompleto`: ele virou `ComoFunciona`.

- [ ] **Step 4: Montar `page.tsx`**

Ordem: `Hero`, `Numeros`, `Areas`, `EncerramSemana`, `TabelaAbertos`, `PorEstado`, `VemAiEDou`, `ComoFunciona`, `BlocoAlerta`. Troque `facetas(hoje)` por `facetas(hoje, { ufs: 27 })`. Apague os imports de `CartaoConcurso`, `LinhaConcurso`, `BlocosSeo` e `Secao` (home). Mantenha `DadosEstruturados`, `metadata`, `revalidate` e o `Promise.all`. Delete `BlocosSeo.tsx`, `Ilustracao.tsx` e `home/Secao.tsx` (confirme antes com `grep -rn "BlocosSeo\|Ilustracao\|home/Secao" src` que ninguém mais importa).

- [ ] **Step 5: Rodar, conferir e fazer o commit**

Run: `pnpm verificar`
Expected: PASS. Compare `http://192.168.15.100:3000` com `Main.dc.html` a 1440 e com `Mobile.dc.html` a 390, bloco a bloco. Confira que nenhum bloco vaza para os lados a 360 px (`document.documentElement.scrollWidth === 360` no console).
```bash
git add -A src/components/home src/app/page.tsx
git commit -m "feat: home completa no visual do protótipo"
```

---

### Task 10: Dados do concurso: fatos, nota comum e também abertos

**Files:**
- Create: `src/lib/fatos.ts`
- Modify: `src/lib/concursos.ts` (nova `tambemAbertos`)
- Test: `src/lib/fatos.test.ts`, `src/lib/concursos.test.ts`

**Interfaces:**
- Produces:
  - `interface Fato { rotulo: "VAGAS" | "REMUNERAÇÃO" | "TAXA" | "CARGA HORÁRIA" | "ESCOLARIDADE" | "CADASTRO RESERVA"; valor: string; apoio: string; informado: boolean }`
  - `fatosDoConcurso(concurso: ConcursoDetalhe): Fato[]` (sempre 6, nessa ordem)
  - `notaComum(cargos: Cargo[]): string | null` ("nível superior, 60 h semanais, taxa de R$ 100,00, remuneração não informada no ato. Requisito: ...")
  - `vagasDoCargo(cargo: Cargo): number | null`
  - `tambemAbertos(concurso: ConcursoResumo, hoje: Date, limite = 3): Promise<ConcursoResumo[]>`

- [ ] **Step 1: Testes (falham)**

```ts
import { describe, expect, it } from "vitest";
import type { Cargo, ConcursoDetalhe } from "./dominio";
import { fatosDoConcurso, notaComum, vagasDoCargo } from "./fatos";

const cargo = (sobre: Partial<Cargo> = {}): Cargo => ({
  nome: "Área X", codigo: null, escolaridade: "superior", area: null, jornadaHoras: 60,
  requisitos: [{ descricao: "Diploma em Medicina Veterinária", formacoes: [] }],
  taxaInscricao: 100, vagas: [{ localidade: null, uf: "RJ", ampla: 2, pcd: 0, negros: 0, outras: 0, total: 2, cadastroReserva: false, crQuantidade: null }],
  remuneracoes: [], evidencia: [], ...sobre,
});

const concurso = (cargos: Cargo[], sobre: Partial<ConcursoDetalhe> = {}) =>
  ({ vagas: 30, taxaInscricao: 100, salarioAte: null, cadastroReserva: false, escolaridades: ["superior"], cargos, ...sobre }) as ConcursoDetalhe;

describe("fatosDoConcurso", () => {
  it("são seis, na ordem do protótipo", () => {
    expect(fatosDoConcurso(concurso([cargo()])).map((f) => f.rotulo)).toEqual([
      "VAGAS", "REMUNERAÇÃO", "TAXA", "CARGA HORÁRIA", "ESCOLARIDADE", "CADASTRO RESERVA",
    ]);
  });

  it("vagas diz em quantas áreas", () => {
    const [vagas] = fatosDoConcurso(concurso(Array.from({ length: 16 }, () => cargo())));
    expect(vagas).toMatchObject({ valor: "30", apoio: "em 16 áreas", informado: true });
  });

  it("remuneração ausente é 'Não informada', e não R$ 0", () => {
    const remuneracao = fatosDoConcurso(concurso([cargo()]))[1];
    expect(remuneracao).toMatchObject({ valor: "Não informada", apoio: "o ato não diz", informado: false });
  });

  it("jornadas diferentes viram faixa", () => {
    const carga = fatosDoConcurso(concurso([cargo({ jornadaHoras: 20 }), cargo({ jornadaHoras: 40 })]))[3];
    expect(carga.valor).toBe("20 a 40 h");
  });

  it("sem cargo nenhum, nada quebra", () => {
    const fatos = fatosDoConcurso(concurso([], { vagas: null, taxaInscricao: null }));
    expect(fatos).toHaveLength(6);
    expect(fatos[0]).toMatchObject({ valor: "a definir", informado: false });
    expect(fatos[2]).toMatchObject({ valor: "Não informada", informado: false });
  });

  it("cadastro reserva sem vaga imediata", () => {
    expect(fatosDoConcurso(concurso([cargo()]))[5]).toMatchObject({ valor: "Não", apoio: "só vagas imediatas" });
  });
});

describe("notaComum", () => {
  it("resume o que vale para todas as áreas", () => {
    expect(notaComum(Array.from({ length: 3 }, () => cargo()))).toBe(
      "nível superior, 60 h semanais, taxa de R$ 100,00, remuneração não informada no ato. Requisito: Diploma em Medicina Veterinária.",
    );
  });

  it("é null quando os cargos diferem ou há um só", () => {
    expect(notaComum([cargo(), cargo({ jornadaHoras: 40 })])).toBeNull();
    expect(notaComum([cargo()])).toBeNull();
  });
});

describe("vagasDoCargo", () => {
  it("soma as vagas e é null sem vaga", () => {
    expect(vagasDoCargo(cargo())).toBe(2);
    expect(vagasDoCargo(cargo({ vagas: [] }))).toBeNull();
  });
});
```
Em `concursos.test.ts`:
```ts
describe("tambemAbertos", () => {
  it("mesma UF, abertos, sem o próprio, no máximo 3", async () => {
    const base = (await listarConcursos({ situacoes: ["abertas"] }, HOJE)).itens.find((c) => c.uf);
    if (!base) return;
    const outros = await tambemAbertos(base, HOJE);
    expect(outros.length).toBeLessThanOrEqual(3);
    for (const c of outros) {
      expect(c.slug).not.toBe(base.slug);
      expect(c.ufs.includes(base.uf!) || c.uf === base.uf).toBe(true);
    }
  });
});
```
Confira a forma de retorno de `listarConcursos` em `concursos.ts:397` e ajuste `.itens` se o nome for outro.

- [ ] **Step 2: Rodar e ver falhar**

Run: `pnpm vitest run src/lib/fatos.test.ts src/lib/concursos.test.ts`
Expected: FAIL.

- [ ] **Step 3: Implementar `fatos.ts`**

```ts
import type { Cargo, ConcursoDetalhe } from "./dominio";
import { moeda, moedaExata, numero } from "./formato";
import { ROTULO_ESCOLARIDADE } from "./rotulos";

export interface Fato {
  rotulo: "VAGAS" | "REMUNERAÇÃO" | "TAXA" | "CARGA HORÁRIA" | "ESCOLARIDADE" | "CADASTRO RESERVA";
  valor: string;
  apoio: string;
  informado: boolean;
}

const NAO_INFORMADA = { valor: "Não informada", apoio: "o ato não diz", informado: false };

export function vagasDoCargo(cargo: Cargo): number | null {
  if (cargo.vagas.length === 0) return null;
  return cargo.vagas.reduce((soma, vaga) => soma + vaga.total, 0);
}

function faixa(valores: number[], formatar: (n: number) => string): string | null {
  if (valores.length === 0) return null;
  const min = Math.min(...valores);
  const max = Math.max(...valores);
  return min === max ? formatar(min) : `${formatar(min)} a ${formatar(max)}`;
}

function remuneracoes(cargos: Cargo[]): number[] {
  return cargos.flatMap((c) => c.remuneracoes.map((r) => r.total ?? r.base)).filter((v): v is number => v !== null && v > 0);
}

export function fatosDoConcurso(concurso: ConcursoDetalhe): Fato[] {
  const { cargos } = concurso;
  const areas = cargos.length;
  const jornadas = cargos.map((c) => c.jornadaHoras).filter((h): h is number => h !== null && h > 0);
  const taxas = [concurso.taxaInscricao, ...cargos.map((c) => c.taxaInscricao)].filter((t): t is number => t !== null && t > 0);
  const salario = faixa(remuneracoes(cargos), moeda) ?? (concurso.salarioAte ? `até ${moeda(concurso.salarioAte)}` : null);
  const escolaridade = concurso.escolaridades[0];
  const formacao = cargos[0]?.requisitos[0]?.formacoes[0] ?? null;
  const reserva = concurso.cadastroReserva || cargos.some((c) => c.vagas.some((v) => v.cadastroReserva));

  return [
    concurso.vagas
      ? { rotulo: "VAGAS", valor: numero(concurso.vagas), apoio: areas > 1 ? `em ${areas} áreas` : areas === 1 ? "em 1 área" : "", informado: true }
      : { rotulo: "VAGAS", valor: "a definir", apoio: "o ato não diz", informado: false },
    salario ? { rotulo: "REMUNERAÇÃO", valor: salario, apoio: "por mês", informado: true } : { rotulo: "REMUNERAÇÃO", ...NAO_INFORMADA },
    taxas.length ? { rotulo: "TAXA", valor: faixa(taxas, moeda)!, apoio: "por inscrição", informado: true } : { rotulo: "TAXA", ...NAO_INFORMADA },
    jornadas.length ? { rotulo: "CARGA HORÁRIA", valor: `${faixa(jornadas, String)} h`, apoio: "por semana", informado: true } : { rotulo: "CARGA HORÁRIA", ...NAO_INFORMADA },
    escolaridade ? { rotulo: "ESCOLARIDADE", valor: ROTULO_ESCOLARIDADE[escolaridade], apoio: formacao ?? "", informado: true } : { rotulo: "ESCOLARIDADE", ...NAO_INFORMADA },
    reserva ? { rotulo: "CADASTRO RESERVA", valor: "Sim", apoio: "além das vagas", informado: true } : { rotulo: "CADASTRO RESERVA", valor: "Não", apoio: "só vagas imediatas", informado: true },
  ];
}

export function notaComum(cargos: Cargo[]): string | null {
  if (cargos.length < 2) return null;
  const [primeiro] = cargos;
  const iguais = <T,>(ler: (c: Cargo) => T) => cargos.every((c) => JSON.stringify(ler(c)) === JSON.stringify(ler(primeiro)));
  if (!iguais((c) => c.escolaridade) || !iguais((c) => c.jornadaHoras) || !iguais((c) => c.taxaInscricao) || !iguais((c) => c.remuneracoes) || !iguais((c) => c.requisitos.map((r) => r.descricao))) return null;

  const partes: string[] = [];
  if (primeiro.escolaridade) partes.push(`nível ${ROTULO_ESCOLARIDADE[primeiro.escolaridade].toLowerCase()}`);
  if (primeiro.jornadaHoras) partes.push(`${primeiro.jornadaHoras} h semanais`);
  if (primeiro.taxaInscricao) partes.push(`taxa de ${moedaExata(primeiro.taxaInscricao)}`);
  const salario = faixa(remuneracoes([primeiro]), moeda);
  partes.push(salario ? `remuneração de ${salario}` : "remuneração não informada no ato");
  const requisito = primeiro.requisitos[0]?.descricao;
  return `${partes.join(", ")}.${requisito ? ` Requisito: ${requisito.replace(/\.$/, "")}.` : ""}`;
}
```
Confira três coisas: que `moedaExata(100)` devolve "R$ 100,00" e `moeda(100)` devolve "R$ 100" (`formato.ts:120-135`); que `ROTULO_ESCOLARIDADE.superior` é "Superior"; e a caixa esperada no teste. Ajuste o teste se o formato real for outro, desde que o texto continue igual ao protótipo.

- [ ] **Step 4: `tambemAbertos` em `concursos.ts`**

```ts
/** "Também abertos em {UF}" na página do concurso: mesma UF, abertos, sem o próprio, por prazo. */
export async function tambemAbertos(concurso: ConcursoResumo, hoje: Date = new Date(), limite = 3): Promise<ConcursoResumo[]> {
  const uf = concurso.uf ?? concurso.ufs[0];
  if (!uf) return [];
  return ordenar(filtrar(await acervo(), { situacoes: ["abertas"], uf }, hoje), "encerrando", hoje)
    .filter((c) => c.slug !== concurso.slug)
    .slice(0, limite);
}
```
Confira o nome do campo de UF no `Filtro` de `filtrar` (em `consulta.ts`).

- [ ] **Step 5: Rodar e ver passar, depois fazer o commit**

Run: `pnpm vitest run src/lib && pnpm typecheck`
Expected: PASS.
```bash
git add src/lib/fatos.ts src/lib/fatos.test.ts src/lib/concursos.ts src/lib/concursos.test.ts
git commit -m "feat: fatos do concurso, nota comum das áreas e também abertos na UF"
```

---

### Task 11: Dados do concurso: inscrição, período e agenda

**Files:**
- Create: `src/lib/inscricao.ts`, `src/lib/agenda.ts`
- Test: `src/lib/inscricao.test.ts`, `src/lib/agenda.test.ts`

**Interfaces:**
- Produces:
  - `interface Passo { titulo: string; detalhe: string }`, `passosDaInscricao(c: ConcursoDetalhe): Passo[]`
  - `interface Periodo { passados: number; total: number; fracao: number }`, `periodoDaInscricao(c: ConcursoResumo, hoje: Date): Periodo | null`
  - `prazoPorExtenso(c: ConcursoDetalhe, hoje: Date): { titulo: string; detalhe: string } | null`, por exemplo `{ titulo: "Encerra amanhã", detalhe: "sexta, às 23h59 (Brasília)" }`
  - `destinoDaInscricao(c: ConcursoDetalhe): { href: string; host: string; rotulo: "Ir para a inscrição" | "Ver o ato publicado" } | null`
  - `icsDoPrazo(c: ConcursoDetalhe, urlDaPagina: string): string | null`

- [ ] **Step 1: Testes (falham)**

`src/lib/inscricao.test.ts`:
```ts
import { describe, expect, it } from "vitest";
import type { ConcursoDetalhe } from "./dominio";
import { destinoDaInscricao, passosDaInscricao, periodoDaInscricao, prazoPorExtenso } from "./inscricao";

const HOJE = new Date(2026, 8, 24);
const base = {
  slug: "ufrrj-51-2026", titulo: "Residência", inscricoesDe: "2026-08-24", inscricoesAte: "2026-09-25",
  taxaInscricao: 100, editalUrl: null, editalCitadoUrl: "https://sigaa.ufrrj.br/inscricao",
  cronograma: [{ tipo: "inscricao", ato: null, inicio: "2026-08-24", fim: "2026-09-25", hora: "23:59", localidades: [], observacao: null, evidencia: null }],
  origens: [{ url: "https://in.gov.br/ato/1" }], cargos: [],
} as unknown as ConcursoDetalhe;

describe("passosDaInscricao", () => {
  it("são três com taxa, e o último traz o prazo", () => {
    expect(passosDaInscricao(base)).toEqual([
      { titulo: "Leia o edital", detalhe: "requisitos e documentos pedidos." },
      { titulo: "Pague a taxa de R$ 100,00", detalhe: "pela guia indicada no edital." },
      { titulo: "Faça a inscrição", detalhe: "até 25/09, 23h59." },
    ]);
  });

  it("sem taxa pula o pagamento, sem prazo não inventa data", () => {
    const passos = passosDaInscricao({ ...base, taxaInscricao: null, inscricoesAte: null, cronograma: [] });
    expect(passos.map((p) => p.titulo)).toEqual(["Leia o edital", "Faça a inscrição"]);
    expect(passos[1].detalhe).toBe("no endereço indicado no edital.");
  });
});

describe("periodoDaInscricao", () => {
  it("conta os dias do período já passados, como no protótipo", () => {
    expect(periodoDaInscricao(base, HOJE)).toEqual({ passados: 31, total: 32, fracao: 31 / 32 });
  });

  it("é null sem as duas datas", () => {
    expect(periodoDaInscricao({ ...base, inscricoesDe: null }, HOJE)).toBeNull();
  });
});

describe("prazoPorExtenso", () => {
  it("amanhã, com dia da semana e hora de Brasília", () => {
    expect(prazoPorExtenso(base, HOJE)).toEqual({ titulo: "Encerra amanhã", detalhe: "sexta, às 23h59 (Brasília)" });
  });

  it("hoje e daqui a alguns dias", () => {
    expect(prazoPorExtenso(base, new Date(2026, 8, 25))?.titulo).toBe("Encerra hoje");
    expect(prazoPorExtenso(base, new Date(2026, 8, 20))?.titulo).toBe("Encerra em 5 dias");
  });

  it("sem hora não inventa 23h59", () => {
    expect(prazoPorExtenso({ ...base, cronograma: [] }, HOJE)?.detalhe).toBe("sexta");
  });
});

describe("destinoDaInscricao", () => {
  it("prefere o endereço do edital e mostra o host", () => {
    expect(destinoDaInscricao(base)).toEqual({ href: "https://sigaa.ufrrj.br/inscricao", host: "sigaa.ufrrj.br", rotulo: "Ir para a inscrição" });
  });

  it("sem endereço cai no ato publicado", () => {
    expect(destinoDaInscricao({ ...base, editalCitadoUrl: null })).toMatchObject({ href: "https://in.gov.br/ato/1", rotulo: "Ver o ato publicado" });
  });
});
```
`src/lib/agenda.test.ts`:
```ts
import { describe, expect, it } from "vitest";
import type { ConcursoDetalhe } from "./dominio";
import { icsDoPrazo } from "./agenda";

const concurso = { slug: "ufrrj-51-2026", titulo: "Residência em Medicina Veterinária", inscricoesAte: "2026-09-25", cronograma: [], orgao: { sigla: "UFRRJ", nome: "Universidade" } } as unknown as ConcursoDetalhe;

describe("icsDoPrazo", () => {
  it("gera um evento de dia inteiro no fim das inscrições", () => {
    const ics = icsDoPrazo(concurso, "https://buscaconcurso.com.br/concursos/ufrrj-51-2026")!;
    expect(ics).toContain("BEGIN:VCALENDAR");
    expect(ics).toContain("DTSTART;VALUE=DATE:20260925");
    expect(ics).toContain("DTEND;VALUE=DATE:20260926");
    expect(ics).toContain("SUMMARY:Fim das inscrições: UFRRJ");
    expect(ics).toContain("UID:ufrrj-51-2026-fim@buscaconcurso.com.br");
    expect(ics.split("\r\n").length).toBeGreaterThan(5);
  });

  it("sem data de fim não gera nada", () => {
    expect(icsDoPrazo({ ...concurso, inscricoesAte: null }, "x")).toBeNull();
  });
});
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `pnpm vitest run src/lib/inscricao.test.ts src/lib/agenda.test.ts`
Expected: FAIL.

- [ ] **Step 3: Implementar `inscricao.ts`**

```ts
import type { ConcursoDetalhe, ConcursoResumo } from "./dominio";
import { dataCurta, diasAte, moedaExata, paraDataLocal } from "./formato";

export interface Passo { titulo: string; detalhe: string }
export interface Periodo { passados: number; total: number; fracao: number }

const SEMANA = ["domingo", "segunda", "terça", "quarta", "quinta", "sexta", "sábado"];

function horaDoFim(c: ConcursoDetalhe): string | null {
  const evento = c.cronograma.find((e) => e.tipo === "inscricao" && e.fim === c.inscricoesAte && e.hora);
  return evento?.hora ? evento.hora.replace(":", "h") : null;
}

export function passosDaInscricao(c: ConcursoDetalhe): Passo[] {
  const passos: Passo[] = [{ titulo: "Leia o edital", detalhe: "requisitos e documentos pedidos." }];
  if (c.taxaInscricao && c.taxaInscricao > 0) {
    passos.push({ titulo: `Pague a taxa de ${moedaExata(c.taxaInscricao)}`, detalhe: "pela guia indicada no edital." });
  }
  const hora = horaDoFim(c);
  passos.push({
    titulo: "Faça a inscrição",
    detalhe: c.inscricoesAte ? `até ${dataCurta(c.inscricoesAte)}${hora ? `, ${hora}` : ""}.` : "no endereço indicado no edital.",
  });
  return passos;
}

export function periodoDaInscricao(c: ConcursoResumo, hoje: Date): Periodo | null {
  if (!c.inscricoesDe || !c.inscricoesAte) return null;
  const total = diasAte(c.inscricoesAte, paraDataLocal(c.inscricoesDe)) + 1;
  if (total <= 0) return null;
  const passados = Math.min(total, Math.max(0, total - diasAte(c.inscricoesAte, hoje)));
  return { passados, total, fracao: passados / total };
}

export function prazoPorExtenso(c: ConcursoDetalhe, hoje: Date): { titulo: string; detalhe: string } | null {
  if (!c.inscricoesAte) return null;
  const dias = diasAte(c.inscricoesAte, hoje);
  if (dias < 0) return null;
  const titulo = dias === 0 ? "Encerra hoje" : dias === 1 ? "Encerra amanhã" : `Encerra em ${dias} dias`;
  const semana = SEMANA[paraDataLocal(c.inscricoesAte).getDay()];
  const hora = horaDoFim(c);
  return { titulo, detalhe: hora ? `${semana}, às ${hora} (Brasília)` : semana };
}

export function destinoDaInscricao(c: ConcursoDetalhe) {
  const edital = c.editalCitadoUrl ?? c.editalUrl;
  const href = edital ?? c.origens.find((o) => o.url)?.url ?? null;
  if (!href) return null;
  let host = "";
  try { host = new URL(href).host; } catch { return null; }
  return { href, host, rotulo: edital ? ("Ir para a inscrição" as const) : ("Ver o ato publicado" as const) };
}
```
Confira `diasAte` com a data de base: `periodoDaInscricao` pede "dias entre inscricoesDe e inscricoesAte". Se `diasAte(iso, hoje)` já normaliza `hoje` para meia-noite local, a conta acima dá 32 no total e 31 passados em 24/09, como no protótipo. Se não bater, corrija a fórmula até o teste passar, sem mexer no teste.

- [ ] **Step 4: Implementar `agenda.ts`**

```ts
import type { ConcursoDetalhe } from "./dominio";
import { nomeCurtoDoOrgao } from "./orgaos";

function escapar(texto: string): string {
  return texto.replace(/\\/g, "\\\\").replace(/;/g, "\\;").replace(/,/g, "\\,").replace(/\n/g, "\\n");
}

function diaSeguinte(iso: string): string {
  const [a, m, d] = iso.split("-").map(Number);
  const data = new Date(Date.UTC(a, m - 1, d + 1));
  return data.toISOString().slice(0, 10).replaceAll("-", "");
}

export function icsDoPrazo(c: ConcursoDetalhe, urlDaPagina: string): string | null {
  if (!c.inscricoesAte) return null;
  const dia = c.inscricoesAte.replaceAll("-", "");
  return [
    "BEGIN:VCALENDAR",
    "VERSION:2.0",
    "PRODID:-//BuscaConcurso//Agenda//PT-BR",
    "BEGIN:VEVENT",
    `UID:${c.slug}-fim@buscaconcurso.com.br`,
    `DTSTAMP:${new Date().toISOString().replace(/[-:]/g, "").slice(0, 15)}Z`,
    `DTSTART;VALUE=DATE:${dia}`,
    `DTEND;VALUE=DATE:${diaSeguinte(c.inscricoesAte)}`,
    `SUMMARY:${escapar(`Fim das inscrições: ${nomeCurtoDoOrgao(c.orgao)}`)}`,
    `DESCRIPTION:${escapar(`${c.titulo}\n${urlDaPagina}`)}`,
    `URL:${urlDaPagina}`,
    "END:VEVENT",
    "END:VCALENDAR",
    "",
  ].join("\r\n");
}
```

- [ ] **Step 5: Rodar e ver passar, depois fazer o commit**

Run: `pnpm vitest run src/lib/inscricao.test.ts src/lib/agenda.test.ts`
Expected: PASS.
```bash
git add src/lib/inscricao.ts src/lib/inscricao.test.ts src/lib/agenda.ts src/lib/agenda.test.ts
git commit -m "feat: passos, período e destino da inscrição e o .ics do prazo"
```

---

### Task 12: Concurso, parte 1: cabeçalho, fatos e ações

**Files:**
- Create: `src/components/concurso/{CabecalhoDoConcurso,FatosDoConcurso,AcoesDoConcurso}.tsx`
- Modify: `src/app/concursos/[slug]/page.tsx:100-230`
- Test: `src/components/concurso/CabecalhoDoConcurso.test.ts`

**Interfaces:**
- Consumes: `fatosDoConcurso`, `icsDoPrazo`, `tomDoConcurso`, `rotuloDeSituacao`, `Selo`, `Etiqueta`, `BotaoEmBreve`, `Trilha`, `urlAbsoluta`.
- Produces:
  - `CabecalhoDoConcurso({ concurso, hoje })` (server)
  - `FatosDoConcurso({ fatos }: { fatos: Fato[] })`
  - `AcoesDoConcurso({ slug, titulo, ics }: { slug: string; titulo: string; ics: string | null })` (cliente): Compartilhar (`navigator.share`, ou `navigator.clipboard.writeText` e o aviso "Link copiado"), Pôr na agenda (baixa `Blob([ics], { type: "text/calendar" })` como `{slug}.ics`; some se `ics` for null) e Salvar (`BotaoEmBreve`).

- [ ] **Step 1: Teste (falha)**

```ts
import { createElement } from "react";
import { renderToStaticMarkup } from "react-dom/server";
import { describe, expect, it } from "vitest";
import { FatosDoConcurso } from "./FatosDoConcurso";

describe("FatosDoConcurso", () => {
  it("fato não informado sai em cinza e com o apoio", () => {
    const html = renderToStaticMarkup(createElement(FatosDoConcurso, {
      fatos: [{ rotulo: "REMUNERAÇÃO", valor: "Não informada", apoio: "o ato não diz", informado: false }],
    }));
    expect(html).toContain("Não informada");
    expect(html).toContain("o ato não diz");
    expect(html).toContain("text-tinta-600");
  });
});
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `pnpm vitest run src/components/concurso/CabecalhoDoConcurso.test.ts`
Expected: FAIL.

- [ ] **Step 3: Implementar a partir de `docs/prototipo/Concurso.dc.html:57-99`** (leia o trecho inteiro)

- Trilha (`:57-61`): Concursos, {sigla do órgão}, {edital}. Reaproveite o `trilha` que a página já monta.
- `CabecalhoDoConcurso` (`:62-89`): `Selo` grande, pílula de situação no tom (`ESTILO_DO_TOM[tom]`, texto "Inscrições abertas · encerram amanhã" composto com `rotuloDeSituacao` e `prazoPorExtenso(...).titulo` em minúsculo), as etiquetas (esfera por `ROTULO_ESFERA`, UF por `NOME_UF`, área `cargos[0]?.area`), `h1` em Bricolage 48 px (`md`) e 30 px (celular), a linha "{nome do órgão} · {título do ato}", e `AcoesDoConcurso` à direita. Mantenha o `Link` do órgão para `/orgaos/{slug}` que a página já tem.
- `FatosDoConcurso` (`:90-99`): `grid grid-cols-2 md:grid-cols-3 xl:grid-cols-6 gap-2`. Cada caixa é `bg-cartao rounded-[16px] shadow-cartao p-4`, com rótulo de 11 px 700 `tracking-[0.05em] text-tinta-500`, valor em 20 px 700 (em `text-tinta-600` e 15 px quando não informado) e apoio de 12 px `text-tinta-600`. No celular os fatos são 4 (Vagas, Taxa, Carga horária e Remuneração, na ordem de `ConcursoMobile.dc.html`), com o ícone em caixa de 36 px ao lado. Implemente com as duas marcações e `md:hidden`/`hidden md:grid`, ou passe `variante`.
- Na `page.tsx`, troque o `Cartao` do cabeçalho e o `BlocoDeNumeros` (linhas 143-225) por `<CabecalhoDoConcurso>` e `<FatosDoConcurso fatos={fatosDoConcurso(concurso)}>`. O `ics` é `icsDoPrazo(concurso, urlAbsoluta(`/concursos/${slug}`))`.

- [ ] **Step 4: Rodar, conferir e fazer o commit**

Run: `pnpm verificar`
Expected: PASS. Abra um concurso aberto no acervo local (pegue um slug em `http://192.168.15.100:3000/concursos?situacao=abertas`) e compare com `Concurso.dc.html` e `ConcursoMobile.dc.html`. Clique em "Pôr na agenda" e confira que o `.ics` baixa e abre no calendário.
```bash
git add src/components/concurso src/app/concursos
git commit -m "feat: cabeçalho, fatos e ações da página do concurso"
```

---

### Task 13: Concurso, parte 2: cronograma, áreas, perguntas, fontes e avaliação

**Files:**
- Modify: `src/components/concurso/{Cronograma,Cargos,Faq,AtosPublicados,Avaliacao}.tsx`, `src/app/concursos/[slug]/page.tsx:230-290`
- Test: `src/components/concurso/Cargos.test.ts` (novo), os testes existentes desses componentes (ajuste de texto)

**Interfaces:**
- Consumes: `notaComum`, `vagasDoCargo`, `Icone`, `Secao`, `hojeCivilEmSaoPaulo`.
- Produces: as mesmas assinaturas de hoje (`Cronograma({ eventos, hoje })`, `Cargos({ cargos })`, `Faq({ origens })`, `AtosPublicados({ origens })`, `Avaliacao({ slug })`). `Cargos` passa a ser cliente (filtro e expandir) ou ganha um filho cliente `ListaDeAreas`.

- [ ] **Step 1: Teste do `Cargos` (falha)**

```ts
import { createElement } from "react";
import { renderToStaticMarkup } from "react-dom/server";
import { describe, expect, it } from "vitest";
import type { Cargo } from "@/lib/dominio";
import { Cargos } from "./Cargos";

const cargo = (nome: string, total: number): Cargo => ({
  nome, codigo: null, escolaridade: "superior", area: null, jornadaHoras: 60, requisitos: [], taxaInscricao: 100,
  vagas: [{ localidade: null, uf: null, ampla: total, pcd: 0, negros: 0, outras: 0, total, cadastroReserva: false, crQuantidade: null }],
  remuneracoes: [], evidencia: [],
});

describe("Cargos", () => {
  it("mostra 8 áreas e o botão com o total", () => {
    const html = renderToStaticMarkup(createElement(Cargos, { cargos: Array.from({ length: 16 }, (_, i) => cargo(`Área ${i + 1}`, 2)) }));
    expect(html).toContain("Área 8");
    expect(html).not.toContain("Área 9<");
    expect(html).toContain("Mostrar as 16 áreas");
    expect(html).toContain("Vale para todas as 16 áreas:");
  });

  it("até 8 áreas não tem botão", () => {
    const html = renderToStaticMarkup(createElement(Cargos, { cargos: [cargo("A", 1), cargo("B", 3)] }));
    expect(html).not.toContain("Mostrar as");
    expect(html).toContain("1 vaga");
    expect(html).toContain("3 vagas");
  });
});
```
O HTML do servidor traz as 8 primeiras áreas visíveis e as outras no DOM com `hidden`, para SEO. Então o teste de "Área 9" confere o `hidden`: troque a segunda asserção por `expect(html).toMatch(/<li[^>]*hidden[^>]*>[\s\S]*?Área 9</)`.

- [ ] **Step 2: Rodar e ver falhar**

Run: `pnpm vitest run src/components/concurso/Cargos.test.ts`
Expected: FAIL.

- [ ] **Step 3: Reestilizar a partir de `docs/prototipo/Concurso.dc.html:100-207`** (leia o trecho inteiro)

- Corpo (`:100-105`): `grid lg:grid-cols-[minmax(0,1fr)_380px] gap-8 px-4 md:px-[112px]`. A coluna principal é uma pilha de cartões `bg-cartao rounded-[20px] shadow-cartao p-8`.
- `Cronograma` (`:106-133`): mantenha a lógica de ordenar e datar. Visual: linha vertical `border-l-2 border-linha` com marcadores de 24 px (passado: círculo `bg-acao` com `check` branco; hoje: a pílula "HOJE" `bg-tinta-900 text-cartao` com "{data} · falta N dia(s) para encerrar"; próximo marco: anel `border-[2.5px] border-urucum bg-urucum-fundo` e texto `text-urucum-texto font-bold`; futuro: anel `border-linha`). A linha "Lido de: {evidencia} · ver o ato" sai de `evento.evidencia` e o link para a origem de `evento.ato`, quando houver. Datas à direita, em `numero`.
- `Cargos` (`:134-156`): o título "Áreas e vagas ({n})" (use `tituloDosCargos` se ele já diz "Cargos e vagas" para um só cargo), o campo "Filtrar áreas" (cliente, filtra por `nome` sem acento com `normalizar`), a nota `bg-rebaixada rounded-[14px] p-4` com "Vale para todas as N áreas:" em negrito e `notaComum(cargos)`, e a tabela `grid-cols-[1fr_120px_150px]` (ÁREA, VAGAS com os pontos `●` repetidos até 5 e o rótulo "N vagas", e "De onde foi lido", um link para a evidência). O botão "Mostrar as N áreas" / "Mostrar menos" com `abaixo`/`acima` e `aria-expanded`.
- `Faq` (`:157-179`): acordeão de `<details>` (sem JS), com o primeiro aberto. `summary` de 56 px com o ícone da pergunta em caixa de 36 px `bg-rebaixada`. A resposta é um `<blockquote>` com aspas “ ”, 15 px, `border-l-[3px] border-acao pl-4`, mais "Ver no ato publicado". No fim, "O ato não responde:" com a lista. Ícones por `FaqPergunta`: `quem_pode` vira `vagas`, `ate_quando` vira `previsto`, `quanto_custa` vira `salario`, `onde_inscrever` e `como_inscrever` viram `globo`, `etapas_prova` vira `lupaDocumento`.
- `AtosPublicados` (`:180-196`): o título "Fontes" e os dois cartões lado a lado. "CONFERIDO POR NÓS" em `text-verde-texto`, título "Ato no Diário Oficial da União", linha "{titulo} · {caracteres} caracteres · coletado em {data}. Pode ser o extrato, não o edital completo." e `BotaoLink secundario` "Ler o ato na íntegra". "INFORMADO PELO ATO · NÃO CONFERIDO" em `text-ouro-sinal-texto`, "Edital completo no site do {sigla}", o host e `BotaoLink contorno` "Abrir no site do {sigla}". Mantenha a lógica de escolher e ordenar as origens.
- `Avaliacao` (`:197-207`): cartão com "Esta página está certa?" e o apoio. Os botões "Sim" e "Não, tem erro" ficam `bg-rebaixada`; o escolhido vira `bg-acao text-acao-texto` (sim) ou `bg-urucum text-white` (não). Mantenha o POST e os estados existentes.

- [ ] **Step 4: Rodar, conferir e fazer o commit**

Run: `pnpm verificar`
Expected: PASS (ajuste os testes existentes desses componentes só onde o texto mudou de propósito). Compare com `Concurso.dc.html` num concurso com vários cargos.
```bash
git add src/components/concurso src/app/concursos
git commit -m "feat: corpo da página do concurso no visual do protótipo"
```

---

### Task 14: Concurso, parte 3: lateral e celular

**Files:**
- Create: `src/components/concurso/{LateralDoConcurso,PassosDaInscricao,TambemAbertos,AbasDoConcurso,BarraDeInscricao}.tsx`
- Modify: `src/app/concursos/[slug]/page.tsx`, `src/components/layout/Cabecalho.tsx` (header do concurso no celular)
- Test: `src/components/concurso/PassosDaInscricao.test.ts`

**Interfaces:**
- Consumes: `passosDaInscricao`, `periodoDaInscricao`, `prazoPorExtenso`, `destinoDaInscricao`, `tambemAbertos`, `Calendario`, `BotaoEmBreve`, `BlocoAlerta`.
- Produces:
  - `LateralDoConcurso({ concurso, hoje, tambem })` (server)
  - `PassosDaInscricao({ slug, passos })` (cliente): cada passo é um `<button aria-pressed>`, e o estado fica em `localStorage["buscaconcurso.passos." + slug]` como array de índices, com `try/catch` na leitura e na escrita
  - `AbasDoConcurso({ paineis: { id, rotulo, conteudo: ReactNode }[] })` (cliente, só `< lg`)
  - `BarraDeInscricao({ taxa, destino })`: `fixed bottom-0` só `< lg`

- [ ] **Step 1: Teste (falha)**

```ts
import { createElement } from "react";
import { renderToStaticMarkup } from "react-dom/server";
import { describe, expect, it } from "vitest";
import { PassosDaInscricao } from "./PassosDaInscricao";

describe("PassosDaInscricao", () => {
  it("renderiza no servidor com todos desmarcados", () => {
    const html = renderToStaticMarkup(createElement(PassosDaInscricao, {
      slug: "x", passos: [{ titulo: "Leia o edital", detalhe: "requisitos." }, { titulo: "Faça a inscrição", detalhe: "até 25/09." }],
    }));
    expect(html.match(/aria-pressed="false"/g)).toHaveLength(2);
    expect(html).toContain("Leia o edital");
  });
});
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `pnpm vitest run src/components/concurso/PassosDaInscricao.test.ts`
Expected: FAIL.

- [ ] **Step 3: Implementar a partir de `docs/prototipo/Concurso.dc.html:208-260`, `:324-340` e `ConcursoMobile.dc.html`**

- `LateralDoConcurso` (`:208-260`): `aside` `hidden lg:flex flex-col gap-4 sticky top-6 self-start`. Cartão principal: `Calendario tamanho="lg"`, `prazoPorExtenso` (título em Bricolage 26 px na cor do tom e detalhe abaixo), "Abriu {dd/mm} · Fecha {dd/mm}", a barra de progresso (`h-1.5 rounded-full bg-urucum/18` com o preenchimento `bg-urucum` em `width: fracao*100%`, `role="progressbar"` com `aria-valuenow`, `aria-valuemin`, `aria-valuemax`) e "{passados} de {total} dias do período já passaram". Depois o `BotaoLink` `chamada` `xl` de largura total com `externo` (`destino.rotulo`, `target="_blank" rel="noopener"`) e "Você vai para o {host}, endereço lido do ato." Sem `prazo` (previsto): título "Previsto", sem barra, com o CTA `BotaoEmBreve recurso="alertas"` "Avisar quando abrir". Encerrado: título "Inscrições encerradas", sem CTA de inscrição.
  - "PARA SE INSCREVER" com `PassosDaInscricao`: círculo de 24 px (marcado: `bg-acao` com `check`), título 600 e detalhe `text-tinta-600`; marcado vira `line-through` no título.
  - Os botões Salvar (`BotaoEmBreve salvos`) e Lembrar amanhã (`BotaoEmBreve lembrete`) em `grid-cols-2`.
  - `TambemAbertos`: "TAMBÉM ABERTOS NO {NOME_UF}", 3 linhas com selo de 36 px, nome, cargo e "{salário ou banca ou vagas} · até {dd/mm}", cada uma link. Some com lista vazia.
  - O alerta compacto: o `BlocoAlerta` com a prop nova `compacto` e o título "Avise-me de novas {área ou cargo principal}".
- `AbasDoConcurso` (celular, `ConcursoMobile.dc.html`, bloco `tablist`): as abas Cronograma, Áreas ({n}) e Perguntas. Os três painéis já estão renderizados no servidor pela `page.tsx`; o componente só aplica `hidden` nos inativos, com `role="tabpanel"` e `aria-labelledby`, e setas esquerda e direita trocam de aba. Em `lg` as abas somem e os painéis ficam todos visíveis (`lg:block`).
- `BarraDeInscricao` (fim de `ConcursoMobile.dc.html`): `fixed inset-x-0 bottom-0 lg:hidden bg-cartao px-4 pt-3 pb-6 shadow-[0_-1px_0_var(--color-linha),0_-12px_30px_rgb(15_31_23/0.08)]`, com a taxa (`moedaExata` ou "Sem taxa") e o botão `chamada` de 52 px. Some se `destino` for null. A `page.tsx` adiciona `pb-28 lg:pb-0` no `main` do concurso para o rodapé não ficar embaixo da barra.
- O cartão de urgência do celular (`ConcursoMobile.dc.html`, o bloco `#FBE7E0`): ícone `prazo` em caixa urucum, `prazoPorExtenso` e a barra, só `< lg`.
- Header do celular (voltar, logo, compartilhar, salvar): no `Cabecalho`, quando `usePathname()` começa com `/concursos/`, abaixo de `lg` a nav vira a de 60 px de `ConcursoMobile.dc.html:21-27`. O voltar usa `router.back()` quando `document.referrer` é do mesmo host, senão vai para `/concursos`. Compartilhar reaproveita a lógica de `AcoesDoConcurso`, extraída para `useCompartilhar()`.

- [ ] **Step 4: Montar na `page.tsx`**

Busque `tambem = await tambemAbertos(concurso, hojeCivil)` junto com o resto (numa falha, `[]` e log `[concurso]`). A coluna principal vira `<AbasDoConcurso paineis={[{ id: "cronograma", ... }, { id: "areas", ... }, { id: "perguntas", ... }]} />` seguida de Fontes e Avaliação (fora das abas). Depois vêm `<LateralDoConcurso>` e `<BarraDeInscricao>`.

- [ ] **Step 5: Rodar, conferir e fazer o commit**

Run: `pnpm verificar`
Expected: PASS. A 1440 px: a lateral acompanha a rolagem. A 390 px: header de 60 px, cartão de urgência, fatos 2×2, abas funcionando por clique e por seta, e a barra fixa sem cobrir o rodapé. Marque um passo, recarregue e confira que ele continua marcado.
```bash
git add src/components/concurso src/components/layout/Cabecalho.tsx src/app/concursos
git commit -m "feat: lateral de inscrição e versão celular da página do concurso"
```

---

### Task 15: Páginas herdadas

**Files:**
- Modify: `src/components/busca/{ColunaFiltros,ListaDeResultados,ResultadosDaBusca,LinkDaConsulta}.tsx`, `src/components/concurso/{LinhaConcurso,CartaoConcurso}.tsx`, `src/app/concursos/page.tsx`, `src/app/busca/[termo]/page.tsx`, `src/app/orgaos/[slug]/page.tsx`, `src/components/auth/{AuthShell,AuthUi,PublicAuthScreens,AccountScreen}.tsx`, `src/app/{not-found,error,global-error}.tsx`, `src/app/opengraph-image.tsx`, `src/app/estilo/{page.tsx,exemplos.ts}`, `src/components/ui/Sugestoes.tsx`
- Test: os existentes (`concursos.test.ts` renderiza o `CartaoConcurso`), com ajuste só de texto mudado de propósito

**Interfaces:**
- Consumes: tudo das Tasks 2 a 5.
- Produces: nenhuma interface nova.

- [ ] **Step 1: Lista e busca**

- `CartaoConcurso` passa a ser o cartão da lista no celular de `Mobile.dc.html` ("Abertos agora"), e `LinhaConcurso` passa a ser a linha da tabela de `Main.dc.html:208-265` como `<div role="row">` dentro de uma lista (a lista de busca não é tabela, porque tem paginação e filtros). Uma linha da busca repete o markup da `TabelaAbertos` (Task 9): extraia a linha para `LinhaConcurso` e faça a `TabelaAbertos` usá-la também, para não duplicar.
- `ColunaFiltros`: cartão `bg-cartao rounded-[20px] shadow-cartao p-5`, grupos com o `Rotulo` da Task 5, caixas de seleção de 20 px `accent-[var(--color-acao)]`, contagem à direita em `text-tinta-500`. As situações viram `Abas` no topo da lista.
- `/concursos` e `/busca/[termo]`: o cabeçalho da página com o `Secao` (rótulo "INSCRIÇÕES ABERTAS" ou "BUSCA", `h1` 40 px) e o grid `lg:grid-cols-[300px_minmax(0,1fr)] gap-6 px-4 md:px-[112px] py-12`. `Paginacao` e `Sugestoes` com o visual da Task 5.
- Mantenha toda a lógica, a navegação SPA e os parâmetros.

- [ ] **Step 2: Órgão**

`/orgaos/[slug]`: o cabeçalho com `Selo` grande, `h1` e a linha de contexto (`linhaDeContexto`), três fatos (abertos, previstos e total de concursos, na mesma caixa de `FatosDoConcurso`) e a lista com `LinhaConcurso`.

- [ ] **Step 3: Autenticação**

`AuthShell`: `min-h-[calc(100vh-112px)] grid lg:grid-cols-[1fr_520px]`. À esquerda, `Azulejos` com `FAIXA_MARCA` numa coluna `bg-faixa` com o `Logo variante="empilhado" tom="claro"` e a frase do produto. À direita, o cartão `bg-cartao rounded-painel shadow-cartao p-8` com o formulário. No celular fica só o cartão. `AuthUi`: campos da Task 5, botão `primario` `lg` de largura total e os botões de Google e LinkedIn em `contorno`. Mantenha toda a lógica de `PublicAuthScreens` e `AccountScreen`.

- [ ] **Step 4: Erros e OpenGraph**

- `not-found.tsx` e `error.tsx`: o `Secao` com rótulo ("PÁGINA NÃO ENCONTRADA" ou "ALGO DEU ERRADO"), frase curta, `BotaoLink primario` para `/` e busca, e `Azulejos` 4×2 à direita.
- `global-error.tsx`: só troque as cores e a fonte inline para Papel `#F6F4EE`, tinta `#0F1F17` e o botão `#0B6B3A`, com a fonte `system-ui`. Ali não há layout nem CSS global.
- `opengraph-image.tsx`: leia o guia de `ImageResponse` em `node_modules/next/dist/docs/`. Fundo `#0A4D2E`, o símbolo do logo à esquerda (o SVG inline da Task 1), "Encontre seu concurso." em branco e "Direto do edital." em `#F2C230`, e uma faixa de azulejos à direita feita com `div` e `border-radius` (Satori não tem grid: use `display: flex` com `flexWrap`). A fonte Bricolage é carregada por `fetch` do Google Fonts, como o arquivo atual já faz com Literata (confira como está hoje e troque a família).

- [ ] **Step 5: `/estilo`**

Reescreva `src/app/estilo/page.tsx` e `exemplos.ts` como vitrine, na ordem de `docs/prototipo/Logo.dc.html`: marca (horizontal, empilhado, variações do símbolo, favicon), faixa de azulejos, paleta (as 6 cores com nome, hex e uso, mais a tabela dos tons de sinal), ícones (todos de `ICONES` em caixa de 52 px com o nome), tipografia (Bricolage 56 px e Public Sans com os números tabulares), e depois cada primitivo com as variantes: `Botao` (5 variantes × 4 tamanhos), `Etiqueta` (5 tons), `Calendario` (hoje, amanhã, depois), `Abas`, `Selo`, `Cartao`, `Campo`, `Trilha`, `Paginacao` e `BotaoEmBreve`. Mantenha o `noindex` que a página tiver.

- [ ] **Step 6: Verificar e fazer o commit**

Run: `pnpm verificar`
Expected: PASS. Passe por `/concursos`, `/busca/professor`, um `/orgaos/<slug>`, `/entrar`, `/cadastrar`, `/conta` (logado), `/pagina-que-nao-existe` e `/estilo`, nos dois temas.
```bash
git add -A src
git commit -m "feat: busca, órgão, autenticação, erros, OpenGraph e /estilo no visual novo"
```

---

### Task 16: Verificação final

**Files:**
- Modify: `scripts/verificar-navegador.mjs`
- Create: `docs/superpowers/verificacao/2026-09-25-redesign/*.png` (os screenshots)

**Interfaces:**
- Consumes: o app inteiro.

- [ ] **Step 1: Estender o `verificar-navegador.mjs`**

- `LARGURAS = [360, 375, 390, 1440]`.
- Páginas: `/`, `/concursos`, `/busca/professor`, `/entrar`, `/em-breve/salvos`, e o primeiro `/concursos/<slug>` lido de `/concursos?situacao=abertas` (primeiro `href` que começa com `/concursos/`).
- Novas conferências por página e largura: `scrollWidth <= innerWidth` (já existe para as três primeiras larguras), nenhum `console.error` e nenhum `pageerror`, e a 1440 o seletor `nav[aria-label="Principal"]` visível. Na home a 1440, `#estados [data-uf]` tem 27 elementos.
- No concurso a 390, `[role="tablist"]` existe e, depois de clicar na segunda aba, o painel de áreas fica visível.
- Screenshot de página inteira (`Page.captureScreenshot` com `captureBeyondViewport: true`) da home e do concurso a 390 e 1440, salvo em `docs/superpowers/verificacao/2026-09-25-redesign/{pagina}-{largura}.png`.

- [ ] **Step 2: Build de produção e verificação**

```bash
BC_API_URL=http://127.0.0.1:8788/v1 NEXT_PUBLIC_BC_API_URL=http://127.0.0.1:8788 pnpm build
BC_API_URL=http://127.0.0.1:8788/v1 NEXT_PUBLIC_BC_API_URL=http://127.0.0.1:8788 pnpm start -H 0.0.0.0 -p 3100 &
pnpm verificar:navegador http://127.0.0.1:3100
```
(Confira os valores das variáveis em `.env.local`.)
Expected: todas as linhas `ok`, sem nenhuma `FALHA`.

- [ ] **Step 3: Grep dos hex fixos e do travessão**

```bash
grep -rnE "#[0-9A-Fa-f]{6}\b" src/components src/app --include=*.tsx | grep -v "marca/Azulejos\|marca/Logo\|mapaUf\|opengraph-image\|global-error\|BarraUtilitaria"
grep -rn $'\u2014' src docs/superpowers/specs/2026-09-25-redesign-design.md docs/superpowers/plans/2026-09-25-redesign.md
```
Expected: nenhuma linha. Cada hex que aparecer vira token.

- [ ] **Step 4: Comparação visual**

Abra os PNGs ao lado de `docs/prototipo/Main.dc.html`, `Mobile.dc.html`, `Concurso.dc.html` e `ConcursoMobile.dc.html` (renderize cada artboard no Chrome headless com o mesmo script, carregando o arquivo com `file://`: a tag `support.js` falha, mas o markup é HTML puro com estilo inline e renderiza). Liste as diferenças de medida, cor ou texto e corrija o que não for dado real diferente do protótipo.

- [ ] **Step 5: Commit e PR**

```bash
git add scripts/verificar-navegador.mjs docs/superpowers/verificacao
git commit -m "test: verificação no navegador do redesign a 360, 390 e 1440 px"
git push -u origin redesign
gh pr create --title "Redesign: design system novo, home e página do concurso" --body "$(cat <<'EOF'
Implementa docs/superpowers/specs/2026-09-25-redesign-design.md.

- Design system novo: paleta Verde Mata, Ouro, Anil, Urucum e Papel; Bricolage Grotesque e Public Sans; logo da lupa sobre o azulejo; ícones Lucide; tema escuro derivado.
- Home e página do concurso fiéis ao protótipo (docs/prototipo), desktop e celular.
- Busca, órgão, autenticação, erros, OpenGraph e /estilo na mesma linguagem.
- Recursos sem backend (salvos, áreas, Diário Oficial, alertas, alto contraste) como placeholder em /em-breve.

Screenshots em docs/superpowers/verificacao/2026-09-25-redesign.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01H8JivVPNNoz3yuBPUoaskM
EOF
)"
```
Não faça o merge: o PR espera a aprovação do usuário.
